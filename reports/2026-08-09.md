# Vulnerability Watch — 2026-08-09

## Summary
| Library | Finding | Severity | Verdict | Official fix |
|---|---|---|---|---|
| Apache Camel | ✅ No new findings | — | — | — |

## Findings

No new findings this run. Incremental window: `last_run` (2026-08-08T04:09:06Z) minus 48h ⇒ swept since **2026-08-06T04:09:06Z** (with the commit sweep widened to 2026-08-04 for margin).

Everything that surfaced during today's sweep was either:
- **Already in `state/seen.json`** from a prior run (identifier-deduped): e.g. `CAMEL-24296` (JEP-290 `CamelObjectInputStream` filter, seen 2026-08-07), `CAMEL-24298`/`CAMEL-24360` (seen 2026-08-06/2026-08-08), and repeat web-search hits for previously-recorded CVEs (`CVE-2026-49042`, `CVE-2026-40860`, `CVE-2026-27172`, `CVE-2026-33454`, `CVE-2026-46456`, `CVE-2026-48206`, `CVE-2026-46590`, `CVE-2026-53913`, `CVE-2026-33453`, `CVE-2025-27636`).
- **Not Apache Camel at all**, despite surfacing in a `camel`-adjacent web search — checked individually and dropped without recording: `CVE-2026-41293` (Apache Tomcat), `CVE-2026-58052` (7-Zip for Windows), `CVE-2026-0994` (Google protobuf, Python).
- **Not security-relevant** on inspection: PR #25360 / `CAMEL-24321` ("Allow stream caching spool directory to be resolved per-Exchange") matched the silent-fix keyword sweep (`spool`) again but remains a pure backward-compatible SPI extension — already assessed `NOT-SECURITY` in the 2026-08-08 run, not re-recorded.

The `apache/camel` GitHub Security Advisories page (`github.com/apache/camel/security/advisories`) was checked directly and explicitly states there are no published advisories on that page (Camel's advisories are published via `camel.apache.org/security/`, not GHSA-native for this repo, consistent with prior runs' findings).

No genuinely new CVE/GHSA/Jira identifier was found for `apache-camel` in the lookback window.

## Delivery

_(filled in after the delivery attempt below)_

## Source health

- **camel.apache.org** (security page): **FAILED** — blocked at the session's network egress policy (`CONNECT tunnel failed, response 403`, confirmed via the proxy status endpoint `recentRelayFailures`). Same failure mode as every run since ~2026-07-19.
- **issues.apache.org** (Jira REST API + web UI): **FAILED** — same egress-policy 403, confirmed via the proxy status endpoint. No direct JQL sweep of `project = CAMEL AND labels in (security) ...` was possible.
- **NVD** (`services.nvd.nist.gov`): **FAILED** — same egress-policy 403, confirmed via the proxy status endpoint.
- **GitHub REST/Search API** (`api.github.com`): **PARTIAL** — direct `curl`/WebFetch to `api.github.com` return 403 (this session's git-proxy layer requires the target repo to be attached for API access, and `apache/camel` is only available for anonymous git-protocol reads, not API/attach). However, the `github` MCP server's global `search_issues` / `search_commits` tools (not repo-attach-gated) worked normally and returned 0 merged security-flagged PRs / 0 CVE-mentioning commits for the lookback window — used as a cross-check against the commit sweep below.
- **git protocol to github.com** (`git clone`) and **github.com web pages via WebFetch**: **OK** — a `--shallow-since=2026-08-04` clone of `apache/camel` (`main`) was swept (`git log --since=2026-08-05`, 81 commits) for CVE/security/vulnerability/CWE/deserialization/injection-style keywords in commit subjects; only one match, an already-seen Jira key. `github.com/apache/camel/security/advisories` was also reachable via WebFetch and confirmed empty.
- **WebSearch**: **OK** — used for GHSA/CVE discovery and cross-referencing; several queries returned only previously-seen identifiers plus three unrelated (non-Camel) CVEs, confirmed and dropped (see Findings).
- **Power Automate delivery webhook**: see Delivery above/below.

**Caveat:** as in every run since 2026-07-19, the four primary structured sources named in `AGENT.md` (Camel security page, Jira, NVD, GHSA-by-ecosystem via `api.github.com`) remain blocked by this session's network egress policy. Today's sweep substituted a full commit-message sweep of the incremental window via git-protocol clone, the GitHub web UI's advisories page, the `github` MCP server's global search tools, and web search — consistent with the fallback methodology used in the 2026-08-05 through 2026-08-08 runs. This is materially less systematic than a fully reachable structured sweep and cannot positively rule out an advisory that never surfaces in a Jira commit message or a web search snippet. **This is a recurring, multi-week infrastructure issue, not specific to today** — see prior reports' "Recurring issue" sections for the standing recommendation to widen this environment's egress allowlist.
