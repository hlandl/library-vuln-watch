# Vulnerability Watch — 2026-08-17

## Summary

✅ No new findings.

## Findings

None. No new CVE/GHSA advisories, Jira security tickets, or "silent fix" commits were identified for Apache Camel (pinned 4.8.8) in this run's window.

## Method notes

- **Lookback window:** incremental mode. `state/seen.json` `last_run` = `2026-08-16T04:03:22Z`; window swept = everything since `2026-08-14T04:03:22Z` (48h overlap per methodology step 1).
- **GHSA sweep** (`github.com/advisories?query=type:reviewed+ecosystem:maven+camel`, via WebFetch, sorted newest): 25-entry Maven/camel-scoped page retrieved (of 56 total reviewed advisories in the database), newest entry unchanged (`GHSA-hcm8-x79p-wx2w` / `CVE-2026-48205`, Jul 6, 2026). Every GHSA ID on the page cross-checked against `state/seen.json` — all already recorded (`AFFECTS_US` or `NOT_RELEVANT`). Nothing new.
- **GitHub repo-level advisories** (`github.com/apache/camel/security/advisories`, via WebFetch): "There aren't any published security advisories" — unchanged.
- **Commit sweep**: shallow-cloned `apache/camel` (anonymous git-protocol read, `main`, `--shallow-since=2026-08-13`) and reviewed all 21 commits from `2026-08-14T04:03:22Z` onward, both by subject/body text and by CVE/security/vulnerability/CWE/XXE/injection/traversal/deserialization/RCE/SSRF/auth-bypass/exploit/sanitiz/escape/overflow/privilege keyword grep (case-insensitive, full commit message). 4 raw hits, all verified as substring false positives on inspection:
  - `ca11b8250b66` ("Generated sources regen for SBOM") — matches `RCE` inside "sou**rce**s"; a routine generated-sources regen commit, not security-relevant.
  - `e5ee26c27e6e` (AWS SDK dependency bump #25499) — matches on the dependency module name `software.amazon.awssdk:**security**hub`, a routine version bump (2.51.3→2.51.4) of an SDK module named "securityhub", not a fix. (Same recurring false positive as the 2026-08-16 report; now persisted to `state/seen.json` to stop re-flagging it.)
  - `dccac5e0a82c`, `9e5c6ea7a73c` (camel-tui / GenAI observability and validation-marker work) — match on "**source** labels" / "map option names back to **source** lines" (substring hit on `RCE`). Same recurring false positive noted in prior reports; now persisted.
  No genuine new security-relevant commit. Ticket prefixes touched in-window (CAMEL-24392, -24390, -24373) are all bug-fix/feature/component-addition work (BacklogTracerRouteAdvice stream draining fix, camel-mcp-server wildcard tag matching, Alibaba Cloud Phase 2 components), none security-flagged.
- **Merged-PR sweep** (`github.com/apache/camel/pulls?q=is:pr+is:merged`, via WebFetch): PRs merged in-window (#25491–#25513: TUI/GenAI work, dependency bumps, the BacklogTracerRouteAdvice fix, SBOM regen) reviewed title-by-title — none mention security/CVE/vulnerability/CWE. No security-relevant PR merged in-window.
- **WebSearch sweep** for new CVE/GHSA mentions surfaced only already-known IDs: `CVE-2026-49042`, `CVE-2026-27172` (alias `GHSA-5rc6-9qfp-8vwg`), `CVE-2026-33454`, `CVE-2026-46456`, `CVE-2026-46457`, `CVE-2026-53913`, `CVE-2026-49098`, `CVE-2026-23552`, `CVE-2026-40860`, `CVE-2026-46590` — all already recorded in `state/seen.json` with a verdict. The Camel 4.22.0 LTS release announcement (Aug 11, 2026) was also checked for any newly-disclosed CVEs; the announcement page itself is unreachable (see Source health) and no search result surfaced a CVE tied specifically to that release beyond the already-known IDs above.
- No new fingerprints qualified for step 4 full assessment or step 5 fix-location this run.

## Source health

- **camel.apache.org** (security page, release blog): **FAILED** — `EGRESS_BLOCKED` via WebFetch; direct `curl` also rejected (`CONNECT tunnel failed`, proxy 403). Same failure mode as every run since ~2026-07-19. Substituted with the GitHub-hosted GHSA list, repo-level advisories page, and WebSearch.
- **issues.apache.org** (Jira REST API): **FAILED** — direct `curl` rejected (`CONNECT tunnel failed`, proxy 403). No direct JQL sweep possible; substituted with the `git log` commit sweep, merged-PR listing, and a targeted WebSearch for `site:issues.apache.org` security tickets (no new results).
- **NVD** (`services.nvd.nist.gov` REST API): **FAILED** — direct `curl` rejected (`CONNECT tunnel failed`, proxy 403). Substituted with WebSearch.
- **access.redhat.com** / **advisories.gitlab.com** (RHSA / GHSA-mirror cross-checks): **FAILED** — direct `curl` rejected identically (proxy 403).
- **api.github.com** (direct `curl`, and the `mcp__github__*` GitHub MCP tools): **FAILED** — `apache/camel` is not an attached repository in this session (only `hlandl/library-vuln-watch` is); `add_repo` confirmed anonymous git-protocol read access is available for `apache/camel` but GitHub API/MCP tools require a `push`-level attach, which was not requested (read-only monitoring does not need write credentials). Substituted with the GitHub web UI via WebFetch and anonymous `git clone`.
- **GitHub web UI via WebFetch** (`github.com/advisories`, `github.com/apache/camel/security/advisories`, `github.com/apache/camel/pulls`): **OK** — full Maven/camel GHSA list, repo advisories page, and in-window merged-PR list all retrieved and reconciled.
- **git protocol to github.com** (anonymous shallow clone, `--shallow-since=2026-08-13`): **OK** — 21 commits since 2026-08-14T04:03:22Z reviewed; 4 keyword hits, all confirmed false positives.
- **WebSearch**: **OK** — used to cross-check for newly-published CVEs/GHSAs and Jira security tickets; nothing genuinely new surfaced.
- **Power Automate delivery webhook**: **FAILED** — `CONNECT tunnel failed, response 403` on the initial POST and again on the retry 30 seconds later (`curl --retry 1 --retry-delay 30 --retry-all-errors`, both attempts identical). Same failure class as the blocked sources above — the proxy rejects the CONNECT to this host outright. This continues the standing multi-week delivery outage (at least the 10th consecutive run with a failed delivery, 2026-08-08 through 2026-08-17). Report committed/pushed to the repository regardless; the all-clear status was **not** delivered to email/Teams by the automated channel this run.

**Recurring issue (multi-week, unchanged):** the four primary structured sources named in `AGENT.md` (`camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`, `api.github.com`) remain blocked by this session's network egress policy, same as every run since ~2026-07-19, and RHSA/GitLab-advisories cross-checks are blocked identically. Coverage this run relied on the GitHub web UI (via WebFetch), an anonymous shallow `git clone` of `apache/camel`, and WebSearch — this combination found no new fingerprints this run and reconciled all mentions surfaced by search against `state/seen.json`, but it remains a fallback, not a substitute for the structured sources' completeness guarantees. Standing recommendation unchanged: widen this environment's egress allowlist for `camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`, and `api.github.com`.
