# Vulnerability Watch — 2026-08-18

## Summary

✅ No new findings.

## Findings

None. No new CVE/GHSA advisories, Jira security tickets, or "silent fix" commits were identified for Apache Camel (pinned 4.8.8) in this run's window.

## Method notes

- **Lookback window:** incremental mode. `state/seen.json` `last_run` = `2026-08-17T04:08:27Z`; window swept = everything since `2026-08-15T04:08:27Z` (48h overlap per methodology step 1).
- **GHSA sweep** (`github.com/advisories?query=type:reviewed+ecosystem:maven+camel`, via WebFetch): top-10 (of 56 total reviewed advisories) retrieved, newest entry unchanged (`GHSA-hcm8-x79p-wx2w` / `CVE-2026-48205`, Jul 6, 2026). All IDs already recorded in `state/seen.json`. Nothing new.
- **GitHub repo-level advisories** (`github.com/apache/camel/security/advisories`, via WebFetch): "There aren't any published security advisories" — unchanged.
- **Commit sweep**: shallow-cloned `apache/camel` (anonymous git-protocol read, `main`, `--shallow-since=2026-08-14`) and reviewed all commits since `2026-08-15T04:08:27Z`. Exactly one commit in-window:
  - `ca11b8250b66722c69509ed5074ac5b6a6000141` — "Generated sources regen for SBOM (#25513)" — already recorded in `state/seen.json` as `NOT_RELEVANT` from the prior run (its author-date falls inside both runs' overlap windows); not a new fingerprint, no re-assessment needed.
  No genuine new security-relevant commit in-window.
- **Merged-PR sweep** (`github.com/apache/camel/pulls?q=is:pr+is:merged`, via WebFetch): PRs merged since 2026-08-14 (#25474, #25491–#25503, #25513 — TUI/GenAI work, dependency bumps, the BacklogTracerRouteAdvice fix, Alibaba Cloud components, SBOM regen) reviewed title-by-title — none mention security/CVE/vulnerability/CWE, and all were already reviewed/recorded in prior runs. No new security-relevant PR.
- **WebSearch sweep** for new CVE/GHSA mentions surfaced only already-known IDs (`CVE-2026-49042`, `CVE-2026-46456`, `CVE-2026-46457`, `CVE-2026-33454`, `CVE-2026-40860`, `CVE-2026-33453`) — all already recorded in `state/seen.json` with a verdict. No new Jira security tickets found via `site:issues.apache.org` search.
- No new fingerprints qualified for step 4 full assessment or step 5 fix-location this run.

## Source health

- **camel.apache.org** (security page, release blog): **FAILED** — direct `curl` rejected (`CONNECT tunnel failed`, proxy 403); confirmed via proxy status endpoint as an explicit policy denial (`connect_rejected`, host `camel.apache.org:443`). Same failure mode as every run since ~2026-07-19. Substituted with the GitHub-hosted GHSA list, repo-level advisories page, and WebSearch.
- **issues.apache.org** (Jira REST API): **FAILED** — direct `curl` and `WebFetch` both rejected (`EGRESS_BLOCKED` / proxy 403, confirmed via proxy status endpoint). No direct JQL sweep possible; substituted with the `git log` commit sweep, merged-PR listing, and a targeted WebSearch for `site:issues.apache.org` security tickets (no new results).
- **NVD** (`services.nvd.nist.gov` REST API): **FAILED** — direct `curl` and `WebFetch` both rejected (`EGRESS_BLOCKED` / proxy 403, confirmed via proxy status endpoint). Substituted with WebSearch.
- **api.github.com** (direct `curl`): **FAILED** — proxy 403. `apache/camel` is not an attached repository in this session (only `hlandl/library-vuln-watch` is), and read-only monitoring does not warrant a `push`-level attach. Substituted with the GitHub web UI via WebFetch and anonymous `git clone` (git protocol to `github.com` succeeded — see below).
- **GitHub web UI via WebFetch** (`github.com/advisories`, `github.com/apache/camel/security/advisories`, `github.com/apache/camel/pulls`): **OK** — full Maven/camel GHSA list, repo advisories page, and in-window merged-PR list all retrieved and reconciled.
- **git protocol to github.com** (anonymous shallow clone, `--shallow-since=2026-08-14`): **OK** — 1 commit since 2026-08-15T04:08:27Z reviewed; already a known, previously-assessed fingerprint.
- **WebSearch**: **OK** — used to cross-check for newly-published CVEs/GHSAs and Jira security tickets; nothing genuinely new surfaced.
- **Power Automate delivery webhook**: **FAILED** — `CONNECT tunnel failed, response 403` on the initial POST and again on the retry 30 seconds later (`curl --retry 1 --retry-delay 30 --retry-all-errors`, both attempts identical). Same failure class as the blocked sources above — the proxy rejects the CONNECT to this host outright. This continues the standing multi-week delivery outage (at least the 11th consecutive run with a failed delivery, 2026-08-08 through 2026-08-18). Report committed/pushed to the repository regardless; the all-clear status was **not** delivered to email/Teams by the automated channel this run.

**Recurring issue (multi-week, unchanged):** the three primary structured sources named in `AGENT.md` (`camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`) plus direct `api.github.com` access remain blocked by this session's network egress policy, same as every run since ~2026-07-19. Coverage this run relied on the GitHub web UI (via WebFetch), an anonymous shallow `git clone` of `apache/camel`, and WebSearch — this combination found no new fingerprints this run and reconciled all mentions surfaced by search against `state/seen.json`, but it remains a fallback, not a substitute for the structured sources' completeness guarantees. Standing recommendation unchanged: widen this environment's egress allowlist for `camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`, and `api.github.com`.
