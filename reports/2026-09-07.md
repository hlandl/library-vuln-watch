# Vulnerability Watch — 2026-09-07

## Summary

| Library | Finding | Severity | Verdict | Official fix |
|---|---|---|---|---|
| — | ✅ No new findings | — | — | — |

## Findings

No new findings this run. Every candidate surfaced by today's sweep was already recorded in `state/seen.json` from a prior run, or is not security-relevant / not a used component.

### Commit sweep — `apache/camel` `main`

Window: `2026-09-04T04:09:09Z` → `2026-09-07T04:04:47Z` (48h overlap over `last_run` = `2026-09-06T04:09:09Z`, per methodology step 1). Anonymous shallow clone of `main` (`--depth=1`, then `--depth=1000` fetch to cover the window), 34 commits in range.

Of the 34 commits, 15 carry a `CAMEL-` ticket id and 19 are dependency bumps, doc/xref fixes, or CI/test chores with no ticket id. All 15 ticketed commits' ids (`CAMEL-24622`, `CAMEL-24627`, `CAMEL-24624`, `CAMEL-24615`, `CAMEL-24623`, `CAMEL-24539`, `CAMEL-24559`, `CAMEL-24566`, `CAMEL-24377`, `CAMEL-24601`, `CAMEL-24628`, `CAMEL-24589`, `CAMEL-24614`, `CAMEL-24422`, `CAMEL-24571`, `CAMEL-24560`) were already recorded in `state/seen.json` (most from yesterday's 2026-09-06 run). No new ticket ids in this window.

The 19 un-ticketed commits were keyword-grepped (`CVE|GHSA|security|vulnerab|CWE|exploit|traversal|injection|deserializ|XXE|SSRF|RCE|unauthoriz|authenticat|authoriz|sanitiz|escape|overflow|privilege|bypass|harden|contain|leak|credential|password|secret|expose|unsafe|certificate|TLS|SSL|crypto|symlink`) and reviewed individually — none are security fixes:

- `de59a8a2e` / `3d9eb4bce` — `camel-tahu`: test-fixture leak fix across tests. Test-only; `camel-tahu` not a `used_component`. `NOT-SECURITY`.
- `b44ecb02b` — `camel-opentelemetry-metrics`: fixes ITs broken by OTel exporter split. Test-only; not a `used_component`. `NOT-SECURITY`.
- `3626194f5` — `camel-browse`: fixes `BrowsableQueueTest` queue accumulation across test retries. Test-only correctness fix, no trust-boundary change. `camel-browse` **is** a `used_component`, but this is a test-suite fix, not a vulnerability. `NOT-SECURITY`.
- `44e9db285`, `c3e178ac2`, `024d9388e` — documentation xref/Antora fixes. `NOT-SECURITY`.
- `2cb57c3e6`, `ac09a0c9a`, `ca3624c6d`, `d8b8bfbaf`, `93b876cda`, `50494cb13`, `171116f91`, `89f6c35d7` — routine `chore(deps)` version bumps (maven-surefire-plugin, Debezium, openai-java, cq-alias-fastinstall-quickly-extension, Apache Fory, JLine, Elasticsearch client group, AWS SDK v2). None reference a CVE/security fix in the bump itself, and none of the bumped artifacts are Camel modules in `used_components` (third-party transitive dependencies are out of scope for this watch per `config/libraries.yaml`). `NOT-SECURITY`.
- `b9649aafd` — `camel-docling`: honors the `operationId` URI path segment to select the operation. Functional routing fix; `camel-docling` not a `used_component`. `NOT-SECURITY`.
- `85365d3f7` — Jenkins CI job throttling restore. Infra-only, not shipped code. `NOT-SECURITY`.

### WebSearch general advisory sweep

Ran the same broader WebSearch queries used since 2026-09-02 as a substitute for the still-blocked GHSA/NVD/Jira/security-page APIs ("Apache Camel new CVE advisory September 2026", `"CVE-2026" apache camel security advisory`, a site-scoped freshness query, and a GitHub Advisory Database query). Every CVE/GHSA id surfaced (`CVE-2026-40048`, `CVE-2026-27172`, `CVE-2026-40860`, `CVE-2026-53913`, `CVE-2026-49042`, `CVE-2026-33454`, `CVE-2026-43865`, `CVE-2026-49098`, `CVE-2026-40453`, `CVE-2026-23552`, `CVE-2026-46726`, `CVE-2026-40022`, `CVE-2026-43866`, `CVE-2026-40047`, `CVE-2026-46590`, `CVE-2026-48204`, `CVE-2026-48205`) is **already present in `state/seen.json`** (recorded on runs between 2026-07-15 and 2026-08-25, verdicts unchanged). No new CVE/GHSA fingerprint surfaced this run.

## Carry-over check

Re-checked the four open `NEEDS_REVIEW` carry-overs (`CAMEL-24296`, `CAMEL-24298`, `CAMEL-24299`, `CAMEL-23522`) via WebSearch for a newly-assigned CVE/GHSA: none confirmed — a targeted search for these ticket ids returned no CVE association (only unrelated results, e.g. an unrelated Windows CVE that happens to share the numeral "24296"). No fingerprint or verdict change; all four remain `NEEDS_REVIEW`.

## Method notes

- **Lookback window:** incremental mode, 48h overlap over `last_run` = `2026-09-06T04:09:09Z` → swept since `2026-09-04T04:09:09Z`, per methodology step 1.
- **`apache/camel` commit sweep:** same substitute source as every run since ~2026-07-19 (GHSA/NVD/Jira/security-page direct HTTP access remains blocked — see Source health). Anonymous shallow clone of `main`, 34 commits in range.
- **`camel-4.8.x` maintenance branch:** head unchanged at `5a31b932322777b8eb5f091cf9c6f016a62fe896` (same as every run since 2026-07-23, confirmed via `git ls-remote`) — no backports landed there.
- Repository access: `apache/camel` was cloned read-only for this session (anonymous git clone/fetch via the session's git proxy, not an attached repository); the GitHub REST/GraphQL API and MCP GitHub tools remain unavailable for it (session's API gateway only serves the explicitly-attached `hlandl/library-vuln-watch`), so `api.github.com` (advisories, releases, PR/issue search) is not usable — same limitation as prior runs, worked around the same way (git log substitute + WebSearch).

## Source health

- **camel.apache.org, issues.apache.org, services.nvd.nist.gov**: **FAILED** — egress proxy rejects the CONNECT (`403`, policy denial), reconfirmed this run with direct `curl`. Same failure mode reported in every run since ~2026-07-19 (now 7+ weeks).
- **api.github.com** (GHSA REST endpoint, and `apache/camel` releases/PR/issue search): **FAILED** — `403` via direct `curl`; not usable via the session's repository-scoped API gateway either (`apache/camel` is not an attached-with-push repository).
- **github.com git protocol (`apache/camel` clone/fetch)**: OK.
- **WebSearch**: OK — used for the general advisory-corpus sweep and the carry-over check.
- **`git push`**: OK — report and `state/seen.json` committed and pushed.
- **Power Automate delivery webhook**: **FAILED** — `curl: (56) CONNECT tunnel failed, response 403` (egress-proxy organization-policy denial) on both the initial POST and the retry 30 seconds later. Same failure mode as `camel.apache.org`/`issues.apache.org`/`services.nvd.nist.gov` above — the destination host is not on this session's egress allowlist. This delivery channel has now failed on every run since at least 2026-08-25 (14+ consecutive runs); recommend restoring egress reachability to the Power Automate host or routing delivery through an already-allowed channel.

## Delivery

- Power Automate webhook: **delivery failed** on both the initial POST and the retry 30 seconds later (identical `CONNECT tunnel failed, response 403` policy denial). This run has **0 findings requiring attention** (all-clear), so no notification was missed by this outage — but the outage itself persists and should be fixed so a future run with real findings is not silently lost. Recorded in this report and pushed to the repository.
