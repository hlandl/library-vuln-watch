# Vulnerability Watch — 2026-07-27

**Ninth consecutive day** (2026-07-19 → 2026-07-27) that this run's execution environment
blocks outbound access to `camel.apache.org`, NVD, Apache Jira, the GitHub REST/Search API
(scoped to `apache/camel`), and the delivery webhook host — see **Source health**, unchanged
in kind from the prior eight reports. As on 2026-07-22 → 2026-07-26, the git-protocol/mirror-repo
substitute channel remains reachable and was used again.

**Today's full re-sweep found zero new items**: the `github/advisory-database` mirror's entire
2026 tree (`github-reviewed` + `unreviewed`, January–July) contains the same 16 genuine
`org.apache.camel` Maven advisories recorded in the 2026-07-26 report, and every one resolves —
by its own GHSA id or by its `aliases` CVE id — to a fingerprint already in `state/seen.json`.
No new `apache/camel` git tag or `camel-4.8.x` maintenance-branch commit since the 2026-07-26
check. As an added check this run, a shallow clone (depth 300) of `apache/camel`'s `main`
branch was swept for commits since 2026-07-20 with messages matching `CVE`/`security`/
`vulnerab`/`CWE` (case-insensitive) — ten hits, none of them CVE fixes relevant to 4.8.8: two
are SonarCloud static-analysis rule IDs (`S2068`, `S6506`/`S5144`, unrelated to any CVE), one is
a `main`-only documentation refresh of the security model (no code change), one is a new
JBang-MCP-server feature on `main` (not present in 4.8.8 at all), and the rest are routine
`aws-java-sdk2` dependency bumps. None indicate an unreleased fix for our pinned version.

## How today's data was collected

Direct HTTPS to `camel.apache.org`, `services.nvd.nist.gov`, `issues.apache.org`, and the
delivery webhook host all fail identically to the last eight days (`CONNECT` tunnel `403` via
`curl`; the proxy status endpoint (`$HTTPS_PROXY/__agentproxy/status`) shows no relay-failure
entries and a plain allow-list that does not include any of these hosts — confirmed fresh this
run, not assumed from prior reports). `api.github.com/rate_limit` (no repo scope) succeeds;
`api.github.com/repos/apache/camel` and `api.github.com/advisories?ecosystem=maven` both fail
`403`, as does the `github.com` web UI (`github.com/apache/camel`) — the session repo-scope
restriction described in prior reports is unchanged (this session is scoped only to
`hlandl/library-vuln-watch`). `web.archive.org` was tried as an alternate path to the Camel
security page and also failed with the same `CONNECT` `403` — the block is a general egress
allow-list, not specific to the four named hosts.

Workarounds used, same as the last five days:

1. **`git ls-remote`** against `apache/camel` (plain git transport, not repo-scope-blocked):
   newest `camel-4.8.x` tag is still **`camel-4.8.9`**; the `camel-4.8.x` maintenance branch
   head is still `5a31b932322777b8eb5f091cf9c6f016a62fe896` — both unchanged from 2026-07-23
   through 2026-07-26.
2. **Sparse, blob-filtered clone of `github/advisory-database`** (`--filter=blob:none`,
   sparse-checkout of `advisories/{github-reviewed,unreviewed}/2026`) — 53 files mention
   "camel" (case-insensitive), same count as the last two days. Of those, 16 are genuine
   `Maven` / `org.apache.camel:*` advisories (verified by parsing `affected[].package`, not
   just the keyword hit) — the same 16 as 2026-07-26, all already resolving to fingerprints in
   `state/seen.json` (by GHSA id or by CVE alias). The remaining 37 are the same documented
   false positives (Apache Camel K — different project/ecosystem; unrelated npm/Linux-kernel/
   OpenRemote hits).
3. **`raw.githubusercontent.com`** — reachable, used for spot checks; no new `AFFECTS_US`
   finding surfaced so no backport spot-check was needed.
4. **Shallow clone of `apache/camel` `main`** (`--filter=blob:none --depth 300`) — new this
   run, as a partial substitute for the blocked GitHub PR/commit search: commit-message sweep
   for security-looking keywords since 2026-07-20, see above. This only covers `main`, not the
   `camel-4.8.x` branch (already covered by the unchanged-branch-head check) or closed PRs
   without a landed commit, so it does not fully close the coverage gap below.

This still does not substitute for the Apache Jira sweep (no *SUSPECTED*, no-CVE-yet findings
could be surfaced) or a full GitHub PR/commit keyword search (Search API is repo-scope-blocked;
today's shallow-clone commit sweep is a partial, `main`-branch-only mitigation, not a
replacement). `last_run` in `state/seen.json` is **left unchanged** at
`2026-07-18T04:11:09Z`, for the same reason as the last six reports: it should reflect the last
run that actually covered Jira and the full PR/commit search, so that whichever run regains
access re-sweeps the full outage window for those two sources rather than just from today.

## Summary

✅ No new findings — full parity between today's git-protocol/mirror sweep and the findings
already recorded in `state/seen.json`. (Two pre-existing items remain open from earlier runs
and are unchanged: `CVE-2026-42527` and `CAMEL-23522`, both `NEEDS_REVIEW`; see prior reports
for detail — this run did not re-investigate them, per the methodology's dedup rule.)

## Findings

None new today.

## Apache Camel release / silent-fix check

- `git ls-remote --tags` against `apache/camel`: newest `camel-4.8.x` tag is still
  **`camel-4.8.9`** — unchanged since 2026-07-22, still more than 180 days old.
- `camel-4.8.x` maintenance branch head: `5a31b932322777b8eb5f091cf9c6f016a62fe896` — identical
  to the hash recorded since 2026-07-23; no new commits merged.
- `main`-branch commit-message sweep (see above): no CVE-relevant hits since 2026-07-20.
- Could not perform the full GitHub PR/commit keyword search (`is:merged CVE`/`security`/
  `vulnerability`/`CWE` against `apache/camel`) — Search API is repo-scope-blocked. This remains
  the main coverage gap versus a normal run, unchanged from the last eight days.

## Source health

- **Apache Camel security page** (`camel.apache.org/security/`): **FAILED** — egress-policy
  block (`CONNECT` `403`), 9th consecutive day.
- **NVD** (`services.nvd.nist.gov`): **FAILED** — same block, 9th consecutive day.
- **Apache Jira** (`issues.apache.org`): **FAILED** — same block, 9th consecutive day. No
  Jira-only *SUSPECTED* findings could be surfaced this run.
- **GitHub API** (`api.github.com` — GHSA-by-ecosystem listing, releases, PR/commit search):
  **FAILED** — session repo-scope restriction, unchanged from prior days; `apache/camel` is not
  an authorized repo for this session.
- **GitHub web UI** (`github.com/apache/camel/*`): **FAILED** — same restriction.
- **`web.archive.org`**: **FAILED** — same general egress block (tried this run as an
  alternate path to the Camel security page).
- **`raw.githubusercontent.com`**: **OK** for known paths.
- **`apache/camel` git-protocol transport**: **OK** — used for tag/branch discovery and the
  `main`-branch shallow clone.
- **`github/advisory-database` git-protocol clone**: **OK** — full 2026 tree re-swept, clean
  parity result today (no new Camel-relevant advisories since 2026-07-26).
- **Delivery webhook**: see **Delivery** below.

## Delivery

**Delivery failed.** Both attempts (initial + retry after 30s) to POST this report to the
configured delivery webhook were blocked by the same blanket egress-policy `403` described
above (`curl` exit 56, "CONNECT tunnel failed, response 403") — the webhook host could not be
reached at the network level, not merely rejected by the endpoint itself. No email or Teams
notification could be sent for 2026-07-27 via the normal channel; the on-call is being
notified out-of-band via a push notification from this session instead, and this report plus
the push to `origin/main` are the only record of today's run until delivery is retried. This
is the ninth consecutive day of delivery failure.

## Recurring issue — needs owner attention

Unchanged in substance from the prior eight reports: this scheduled routine has now gone nine
consecutive days (2026-07-19 → 2026-07-27) unable to reach `camel.apache.org`, NVD, Jira, or
the delivery webhook directly. Today's clean parity result again shows the git-protocol/mirror
substitute channel is not itself the problem — but Jira and a full GitHub PR/commit search
remain uncovered for the whole outage window, a real, accumulating coverage gap for *SUSPECTED*
(no-CVE-yet) findings regardless of how well the mirror happens to be caught up on any given
day. Recommended actions, unchanged:

1. Review and widen this environment's outbound egress allowlist to include at minimum:
   `camel.apache.org`, `services.nvd.nist.gov`, `issues.apache.org`, and the Power Automate
   delivery webhook host.
2. Grant this session's GitHub scope access to `apache/camel` (via `add_repo`, with explicit
   owner sign-off) so the GitHub API-based sources (GHSA-by-ecosystem listing, release list,
   PR/commit search) are usable directly instead of via the improvised git-protocol/mirror-repo
   substitute.
