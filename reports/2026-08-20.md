# Vulnerability Watch — 2026-08-20

## Summary

✅ No new findings.

## Findings

None. No new CVE/GHSA advisories, Jira security tickets, or "silent fix" commits were identified for Apache Camel (pinned 4.8.8) in this run's window.

## Method notes

- **Lookback window:** incremental mode. `state/seen.json` `last_run` = `2026-08-19T04:05:51Z`; window swept = everything since `2026-08-17T04:05:51Z` (48h overlap per methodology step 1).
- **GHSA sweep** (`github.com/advisories?query=org.apache.camel`, two independent sort orders, via WebFetch): 7 Maven/camel advisories returned each time (`GHSA-f755-xp6r-8q84`/CVE-2026-43866, `GHSA-w2v8-8q6c-3rhr`/CVE-2026-56140, `GHSA-8h6p-jvhf-9hcr`/CVE-2026-42527, `GHSA-6qw3-4796-5984`/CVE-2026-40859, `GHSA-cmc3-hr79-8mmv`/CVE-2026-46456, `GHSA-xww8-mxqw-m84w`/CVE-2026-43865, `GHSA-2c2h-2855-mf97`/CVE-2025-27636) — all already recorded in `state/seen.json`. Nothing new.
- **GitHub repo-level advisories** (`github.com/apache/camel/security/advisories`, via WebFetch): "There aren't any published security advisories" — unchanged.
- **Commit sweep**: shallow-cloned `apache/camel` (anonymous git-protocol read, `main`, `--shallow-since=2026-08-15`) and confirmed `main`'s HEAD is still `ca11b8250b66722c69509ed5074ac5b6a6000141` ("Generated sources regen for SBOM (#25513)", authored 2026-08-16T17:07:15+02:00) — `git log --since=2026-08-17T04:05:51Z` returned **0 commits**. No commits at all have landed on `apache/camel` main since 2026-08-16, so there is nothing new to assess (that commit is already `NOT_RELEVANT` in `state/seen.json`).
- **Merged-PR sweep** (`github.com/apache/camel/pulls?q=is:pr+is:merged`, via WebFetch, two query variants): returned PRs merged Aug 12–14, 2026 (dependency bumps, new component additions, test infra) — none security-relevant and none referencing a new CAMEL-security Jira ticket. Note: GitHub's search-query filtering (`+security`, `+CVE`) was not reliably honored by the fetched page (results looked unfiltered/generic) — treat this source as **degraded**, not a clean negative.
- **WebSearch sweep** for new CVE/GHSA/Jira mentions (`"apache camel" CVE 2026`, `site:camel.apache.org CVE-2026`, `site:issues.apache.org CAMEL security 2026`, `Apache Camel new CVE vulnerability disclosed August 18 19 20 2026`, plus a targeted follow-up on a Camel-Keycloak issuer-validation finding surfaced by search): every CVE/GHSA id surfaced (`CVE-2026-23552`, `CVE-2026-53913`/`GHSA-qvc3-6q9x-95pj`, `CVE-2026-40860`, `CVE-2026-40048`, `CVE-2026-46726`, `CVE-2026-49042`, `CVE-2026-43865`) was already recorded in `state/seen.json` with a verdict. No new Jira security ticket surfaced.
- No new fingerprints qualified for step 4 full assessment or step 5 fix-location this run.

## Source health

- **camel.apache.org** (security page, release blog): **FAILED** — `WebFetch` rejected (`EGRESS_BLOCKED`). Same failure mode as every run since ~2026-07-19. Substituted with the GitHub-hosted GHSA list, repo-level advisories page, and WebSearch.
- **issues.apache.org** (Jira REST API + browse UI): **FAILED** — `WebFetch` rejected (`EGRESS_BLOCKED`). No direct JQL sweep possible; substituted with the `git log` commit sweep, merged-PR listing, and targeted WebSearch (no new results).
- **NVD** (`services.nvd.nist.gov`) and **cve.org**: **FAILED** — both rejected (`EGRESS_BLOCKED` / `CONNECT tunnel failed`). Substituted with WebSearch and the GHSA page (which carries CVE ids).
- **api.github.com** (direct, unauthenticated): **FAILED** — proxy 403 (`sessions are bound to their configured repositories`). `apache/camel` is not an attached repository in this session (only `hlandl/library-vuln-watch` is, per session Repository Scope); read-only monitoring does not warrant a `push`-level attach of a third-party repo. Substituted with the GitHub web UI via WebFetch and anonymous `git clone` (git protocol to `github.com` succeeded).
- **GitHub web UI via WebFetch** (`github.com/advisories`, `github.com/apache/camel/security/advisories`): **OK** — Maven/camel GHSA list and repo advisories page retrieved and reconciled.
- **GitHub web UI via WebFetch** (`github.com/apache/camel/pulls` search): **DEGRADED** — pages loaded, but query-string keyword filters (`security`, `CVE`) did not appear to narrow results; treated as an unreliable secondary signal only, not a verified negative.
- **git protocol to github.com** (anonymous shallow clone, `--shallow-since=2026-08-15`): **OK** — confirmed no new commits on `main` since 2026-08-16.
- **WebSearch**: **OK** — used to cross-check for newly-published CVEs/GHSAs and Jira security tickets; nothing genuinely new surfaced.
- **Power Automate delivery webhook**: **FAILED** — `CONNECT tunnel failed, response 403` on the initial POST and again on the retry 30 seconds later (`curl --retry 1 --retry-delay 30 --retry-all-errors`, both attempts identical). Same failure class as the blocked sources above — the proxy rejects the CONNECT to this host outright. This continues the standing multi-week delivery outage (at least the 13th consecutive run with a failed delivery, 2026-08-08 through 2026-08-20). Report committed/pushed to the repository regardless; the all-clear status was **not** delivered to email/Teams by the automated channel this run.

**Recurring issue (multi-week, unchanged):** the three primary structured sources named in `AGENT.md` (`camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`) plus direct `api.github.com` access remain blocked by this session's network egress policy, same as every run since ~2026-07-19. Coverage this run relied on the GitHub web UI (via WebFetch), an anonymous shallow `git clone` of `apache/camel`, and WebSearch — this combination found no new fingerprints this run (the upstream repo has in fact been quiet since 2026-08-16), but it remains a fallback, not a substitute for the structured sources' completeness guarantees. Standing recommendation unchanged: widen this environment's egress allowlist for `camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`, and `api.github.com`.
