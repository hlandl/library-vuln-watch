# Vulnerability Watch — 2026-08-21

## Summary

✅ No new findings.

## Findings

None. No new CVE/GHSA advisories, Jira security tickets, or "silent fix" commits were identified for Apache Camel (pinned 4.8.8) in this run's window.

## Method notes

- **Lookback window:** incremental mode. `state/seen.json` `last_run` = `2026-08-20T04:08:35Z`; window swept = everything since `2026-08-18T04:08:35Z` (48h overlap per methodology step 1).
- **GHSA sweep** (`github.com/advisories?query=org.apache.camel`, via WebFetch): 7 Maven/camel advisories returned (`GHSA-w2v8-8q6c-3rhr`/CVE-2026-56140, `GHSA-cmc3-hr79-8mmv`/CVE-2026-46456, `GHSA-8h6p-jvhf-9hcr`/CVE-2026-42527, `GHSA-xww8-mxqw-m84w`/CVE-2026-43865, `GHSA-6qw3-4796-5984`/CVE-2026-40859, `GHSA-f755-xp6r-8q84`/CVE-2026-43866, `GHSA-2c2h-2855-mf97`/CVE-2025-27636) — all already recorded in `state/seen.json`. Nothing new.
- **GitHub repo-level advisories** (`github.com/apache/camel/security/advisories`, via WebFetch): "There aren't any published security advisories" — unchanged.
- **GitHub Releases** (`github.com/apache/camel/releases`, via WebFetch): no releases listed (project doesn't publish via GitHub Releases) — unchanged, nothing to assess.
- **Commit sweep on `main`**: shallow-cloned `apache/camel` (anonymous git-protocol read, `--depth 50`). HEAD is still `ca11b8250b66722c69509ed5074ac5b6a6000141` (2026-08-16) — no commits at all have landed on `main` since then. Nothing new on the default branch.
- **Merged-PR sweep** (`github.com/apache/camel/pulls?q=is:pr+is:merged+sort:updated-desc`, via WebFetch): 2 PRs merged since 2026-08-18 — **#25505** "CAMEL-24392: Fix BacklogTracerRouteAdvice draining WrappedFile streambody (backport 4.18.x)" and **#25557** "chore(ci): Adapt branch camel-4.22.x workflows". Both individually reviewed (WebFetch on the PR page for #25505; #25557 is self-evidently a CI-workflow-only change): neither is security-related — #25505 fixes a stream-caching/resource-handling bug (SFTP `streamDownload` bodies being drained by the backlog tracer), not a vulnerability. Fetched the `camel-4.18.x` branch (git protocol) to confirm the merge commit: `367ce4843827d87fb5016e13c564f0f3a1f0aac0`. Recorded as processed/not-security in state to avoid re-examining.
- **WebSearch sweep** for new CVE/GHSA/Jira mentions (`Apache Camel new CVE vulnerability disclosed August 2026`, `"apache camel" CVE 2026 site:github.com/advisories OR site:nvd.nist.gov`): every CVE/GHSA id surfaced (`CVE-2026-40860`/`GHSA-m5vh-3fw5-5wgh`, `CVE-2026-27172`/`GHSA-5rc6-9qfp-8vwg`, `CVE-2026-46590`, `CVE-2026-33453`, `CVE-2026-33454`, `CVE-2026-40022`, `CVE-2026-23552`, `CVE-2025-66169`/`GHSA-4jrw-64vr-7g8m` [camel-neo4j, not in `used_components`, version range 4.10.0–4.17.0 doesn't include our 4.8.8 pin anyway]) was already recorded in `state/seen.json`. No new Jira security ticket surfaced.
- Apache Jira (`issues.apache.org`) and NVD (`services.nvd.nist.gov`) remain blocked at the network layer (see Source health) — no direct JQL sweep or NVD keyword sweep was possible this run; substituted with the commit/PR sweep and WebSearch as in prior runs.
- No new fingerprints qualified for step 4 full assessment or step 5 fix-location this run.

## Source health

- **camel.apache.org** (security page, release blog): **FAILED** — `WebFetch` rejected (`EGRESS_BLOCKED`). Same failure mode as every run since ~2026-07-19. Substituted with the GitHub-hosted GHSA list, repo-level advisories page, and WebSearch.
- **issues.apache.org** (Jira REST API + browse UI): **FAILED** — `WebFetch` rejected (`EGRESS_BLOCKED`), confirmed again directly with `curl` (`CONNECT tunnel failed, response 403` — proxy-status README classifies 403 as an organization egress-policy denial, not a transient fault). No direct JQL sweep possible; substituted with the `git log` commit sweep, merged-PR listing, and targeted WebSearch (no new results).
- **NVD** (`services.nvd.nist.gov`) and **cve.org**: **FAILED** — both rejected (`EGRESS_BLOCKED` / `CONNECT tunnel failed, response 403`, confirmed directly with `curl` as an org policy denial). Substituted with WebSearch and the GHSA page (which carries CVE ids).
- **api.github.com** (direct, unauthenticated): **FAILED** — proxy 403 (`sessions are bound to their configured repositories`); confirmed directly with `curl` (403). `apache/camel` is not an attached repository in this session (only `hlandl/library-vuln-watch` is, per session Repository Scope); read-only monitoring does not warrant a `push`-level attach of a third-party repo. Substituted with the GitHub web UI via WebFetch and anonymous `git clone`/`git fetch` (git protocol to `github.com` succeeded).
- **GitHub web UI via WebFetch** (`github.com/advisories`, `github.com/apache/camel/security/advisories`, `github.com/apache/camel/releases`, `github.com/apache/camel/pull/25505`): **OK** — advisory list, repo advisories page, releases page, and the one candidate PR retrieved and reconciled.
- **GitHub web UI via WebFetch** (`github.com/apache/camel/pulls` search): **OK this run** — the merged-PR listing returned a small, plausible set (2 PRs since 2026-08-18) and both were individually triaged; treat as a secondary signal, not a substitute for the blocked structured sources.
- **git protocol to github.com** (anonymous shallow clone/fetch, `main` + `camel-4.18.x`): **OK** — confirmed no new commits on `main` since 2026-08-16, and identified/triaged the one relevant commit on `camel-4.18.x`.
- **WebSearch**: **OK** — used to cross-check for newly-published CVEs/GHSAs and Jira security tickets; nothing genuinely new surfaced.
- **Power Automate delivery webhook**: **FAILED** — `CONNECT tunnel failed, response 403` on the initial POST and again on the retry ~30 seconds later. Same failure class as the blocked sources above — the proxy rejects the CONNECT to this host outright. This continues the standing multi-week delivery outage (at least the **14th consecutive run** with a failed delivery, 2026-08-08 through 2026-08-21). Report committed/pushed to the repository regardless; the all-clear status was **not** delivered to email/Teams by the automated channel this run.

**Recurring issue (multi-week, unchanged):** the three primary structured sources named in `AGENT.md` (`camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`) plus direct `api.github.com` access remain blocked by this session's network egress policy, same as every run since ~2026-07-19, and the Power Automate delivery webhook has now failed for 14 consecutive daily runs. Coverage this run relied on the GitHub web UI (via WebFetch), anonymous `git clone`/`git fetch` of `apache/camel`, and WebSearch — this combination found no new fingerprints this run (the upstream `main` branch has in fact been quiet since 2026-08-16, and the one new PR merged elsewhere was confirmed non-security), but it remains a fallback, not a substitute for the structured sources' completeness guarantees. Standing recommendation unchanged: widen this environment's egress allowlist for `camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`, `api.github.com`, and the Power Automate delivery host.
