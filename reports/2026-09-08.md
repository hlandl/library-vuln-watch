# Vulnerability Watch — 2026-09-08

## Summary

| Library | Finding | Severity | Verdict | Official fix |
|---|---|---|---|---|
| — | ✅ No new findings | — | — | — |

## Findings

No new findings this run. Ten new fingerprints surfaced by today's commit sweep (none seen before), all assessed `NOT_RELEVANT` (not security-relevant, or the affected component is not one of ours) — see breakdown below. Every CVE/GHSA id surfaced by the WebSearch advisory sweep was already recorded in `state/seen.json` from a prior run.

### Commit sweep — `apache/camel` `main`

Window: `2026-09-05T04:04:47Z` → `2026-09-07T22:18:28Z` (48h overlap over `last_run` = `2026-09-07T04:04:47Z`, per methodology step 1). Anonymous shallow clone of `main` (`--depth=1`, then `--depth=1000` fetch to cover the window), 45 commits in range.

Of the 45 commits, 20 carry a `CAMEL-` ticket id and 25 are dependency bumps, doc/xref fixes, or CI/test chores with no ticket id.

**10 of the 20 ticketed ids were already recorded in `state/seen.json`** (from the 2026-09-06 run): `CAMEL-24377`, `CAMEL-24539`, `CAMEL-24559`, `CAMEL-24566`, `CAMEL-24601`, `CAMEL-24615`, `CAMEL-24622`, `CAMEL-24623`, `CAMEL-24624`, `CAMEL-24627`.

**10 ticketed ids are new** and were assessed individually (all `NOT_RELEVANT`):

- **`CAMEL-24605`** — `camel-fop`: [`209c67a851`](https://github.com/apache/camel/commit/209c67a851d3c145583d25829b7ffac0c500674b) — `FopProducer` was creating its `TransformerFactory` with only `FEATURE_SECURE_PROCESSING` set; this change additionally sets `ACCESS_EXTERNAL_DTD`/`ACCESS_EXTERNAL_STYLESHEET` to `""`, blocking external-DTD/stylesheet resolution during XSL-FO transformation (an XXE-class hardening, no CVE assigned — flagged `SUSPECTED` per methodology's "silent fix" rule). **`camel-fop` is not in `used_components`** → `NOT_RELEVANT` despite being a real security hardening commit. No further action, but noted here in case `camel-fop` is ever adopted.
- `CAMEL-24632` — `camel-http`: [`f7f2819b50`](https://github.com/apache/camel/commit/f7f2819b50bdea1661598ba97aad80b94b0a71e8) — fixes the proxy-connection scheme wrongly defaulting to `https` for `https` targets (should always be a plain-HTTP `CONNECT` tunnel, independent of the target scheme). A configuration-default correctness fix, not a vulnerability (`NOT-SECURITY`); `camel-http` is also not a `used_component` (only `camel-http-base`/`camel-http-common` are). `NOT_RELEVANT`.
- `CAMEL-24474` — `camel-as2`: [`ba02ad2074`](https://github.com/apache/camel/commit/ba02ad20742f7a3ebd06d44a89d0aadb494b4f82) — adds TLS support for asynchronous MDN delivery (was previously refused outright for `https` addresses). Feature addition, not a fix; `camel-as2` not a `used_component`. `NOT_RELEVANT`.
- `CAMEL-24626` — `camel-master`: [`c379961fa2`](https://github.com/apache/camel/commit/c379961fa226d47171d320e61efaaa3fe247bb94) — fixes a lock-inversion/task-registry leak in leadership handling. Correctness fix, not a vulnerability; `camel-master` not a `used_component`. `NOT_RELEVANT`.
- `CAMEL-24625` — `camel-platform-http`: [`bda1be58a6`](https://github.com/apache/camel/commit/bda1be58a6cd61025ba3a942c7836ab0688466ce) — adds a `stripUriPrefix` option for path-based reverse proxies. Feature addition; `camel-platform-http` not a `used_component`. `NOT_RELEVANT`.
- `CAMEL-24620` — `camel-jackson3`: [`798f778ed0`](https://github.com/apache/camel/commit/798f778ed0616e54d932afb2c9ada2d96558114c) — adds enable/disable support for Jackson 3 `DatatypeFeature` enums. Feature addition; `camel-jackson3` (Jackson-3-based, distinct from `camel-jackson`) not a `used_component`. `NOT_RELEVANT`.
- `CAMEL-24634` — new component `camel-opa` (Open Policy Agent policy evaluation): [`b2741f8fe3`](https://github.com/apache/camel/commit/b2741f8fe3f14d852562a5bb5154a8cb36187ff9). New feature; not a `used_component` (new module). `NOT_RELEVANT`.
- `CAMEL-24631` — `camel doctor`/TUI: [`c211e71acf`](https://github.com/apache/camel/commit/c211e71acf1bd3419b07b9e37cd43b8d0ab1a1b8) — Ollama detection in tooling. `NOT-SECURITY`. `NOT_RELEVANT`.
- `CAMEL-24602` — CI tooling: [`b7659d17f4`](https://github.com/apache/camel/commit/b7659d17f46584a872ae70fc041b581bca7c1d21) — per-module build-time reporting in PR comments. `NOT-SECURITY`. `NOT_RELEVANT`.
- `CAMEL-24618` — test-only: [`b353c1760f`](https://github.com/apache/camel/commit/b353c1760f14249652d9f5f5d9659f5a0822948d) — fixes flaky `SpringFileAntPathMatcherRemoteFileFilterTest` (state contamination + timing race). Touches `camel-file`/`camel-spring` test code (both `used_components`) but is a test-suite reliability fix, not a vulnerability. `NOT-SECURITY`. `NOT_RELEVANT`.

The 25 un-ticketed commits were keyword-grepped (`CVE|GHSA|security|vulnerab|CWE|exploit|traversal|injection|deserializ|XXE|SSRF|RCE|unauthoriz|authenticat|authoriz|sanitiz|escape|overflow|privilege|bypass|harden|contain|leak|credential|password|secret|expose|unsafe|certificate|TLS|SSL|crypto|symlink`) — all are `chore(deps)` bumps of third-party dependencies (parquet, aliyun SDK, Elasticsearch client, Jetty, Debezium, openai-java, various `camel-test-infra-*` container versions, maven-surefire-plugin), CI/test infra fixes (Docling/spring-ai-image `@DisabledIfEnvironmentVariable` fix, drill-jdbc transitive-junit exclusion, a surefire-bump revert, SBOM sources regen), or doc/xref fixes. None reference a CVE/security fix and none touch a Camel module in `used_components` in a security-relevant way. `NOT-SECURITY`.

### WebSearch general advisory sweep

Ran the same broader WebSearch queries used since 2026-09-02 as a substitute for the still-blocked GHSA/NVD/Jira/security-page APIs ("Apache Camel new CVE advisory September 2026", `"CVE-2026" apache camel security advisory`, a site-scoped freshness query, a September-2026-dated query, and a GitHub Advisory Database query). Every CVE/GHSA id surfaced (`CVE-2026-46726`, `CVE-2026-49098`, `CVE-2026-40860`, `CVE-2026-33454`, `CVE-2026-49042`, `CVE-2026-43865`, `CVE-2026-53913`, `CVE-2026-46457`, `CVE-2026-40047`, `CVE-2026-46456`, `CVE-2026-27172` / `GHSA-5rc6-9qfp-8vwg`) is **already present in `state/seen.json`** (recorded on runs between 2026-07-15 and 2026-08-25, verdicts unchanged). No new CVE/GHSA fingerprint surfaced this run.

## Carry-over check

Re-checked the four open `NEEDS_REVIEW` carry-overs (`CAMEL-24296`, `CAMEL-24298`, `CAMEL-24299`, `CAMEL-23522`) via WebSearch for a newly-assigned CVE/GHSA: none confirmed — the targeted search returned only unrelated general advisory pages and CVE database listings, no direct association for any of these four ticket ids. No fingerprint or verdict change; all four remain `NEEDS_REVIEW`.

## Method notes

- **Lookback window:** incremental mode, 48h overlap over `last_run` = `2026-09-07T04:04:47Z` → swept since `2026-09-05T04:04:47Z`, per methodology step 1.
- **`apache/camel` commit sweep:** same substitute source as every run since ~2026-07-19 (GHSA/NVD/Jira/security-page direct HTTP access remains blocked — see Source health). Anonymous shallow clone of `main`, 45 commits in range.
- **`camel-4.8.x` maintenance branch:** head unchanged at `5a31b932322777b8eb5f091cf9c6f016a62fe896` (same as every run since 2026-07-23, confirmed via `git ls-remote`) — no backports landed there.
- Repository access: `apache/camel` was cloned read-only for this session (anonymous git clone/fetch via the session's git proxy, not an attached repository); the GitHub REST/GraphQL API and MCP GitHub tools remain unavailable for it (session's API gateway only serves the explicitly-attached `hlandl/library-vuln-watch`), so `api.github.com` (advisories, releases, PR/issue search) is not usable — same limitation as prior runs, worked around the same way (git log substitute + WebSearch).

## Source health

- **camel.apache.org, issues.apache.org, services.nvd.nist.gov**: **FAILED** — egress proxy rejects the CONNECT (`403`, policy denial), reconfirmed this run with direct `curl`. Same failure mode reported in every run since ~2026-07-19 (now 7+ weeks).
- **api.github.com** (GHSA REST endpoint, and `apache/camel` releases/PR/issue search): **FAILED** — `403` via direct `curl`; not usable via the session's repository-scoped API gateway either (`apache/camel` is not an attached-with-push repository).
- **github.com git protocol (`apache/camel` clone/fetch)**: OK.
- **WebSearch**: OK — used for the general advisory-corpus sweep and the carry-over check.
- **`git push`**: OK — report and `state/seen.json` committed and pushed.
- **Power Automate delivery webhook**: reconfirmed **FAILED** before writing this report — `curl` to the webhook host returns `CONNECT tunnel failed, response 403` (same egress-proxy organization-policy denial as `camel.apache.org` et al.), so delivery below is expected to fail again. This channel has now failed on every run since at least 2026-08-25 (15+ consecutive runs); recommend restoring egress reachability to the Power Automate host or routing delivery through an already-allowed channel.

## Delivery

<!-- filled in after the delivery attempt -->
