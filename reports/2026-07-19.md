# Vulnerability Watch — 2026-07-19

## Summary

⚠️ **No sources could be swept today.** This run's execution environment blocked
outbound network access to every configured data source (see Source health
below). This is **not** a clean bill of health — it means the sweep did not
happen at all, not that no vulnerabilities exist. No new findings were
collected or assessed for Apache Camel 4.8.8 (or any other configured library).

| Library | Finding | Severity | Verdict | Official fix |
|---|---|---|---|---|
| — | — | — | **COLLECTION FAILED — see Source health** | — |

## Findings

None collected. No candidate data was retrieved from any source, so no
assessment (vulnerability confirmation, affected-version check, component-use
check, severity) could be performed.

## Source health

All sources failed identically: the proxy gateway in this run's environment
rejected the outbound `CONNECT` for every external host with `403` ("policy
denial"), confirmed via the agent-proxy status endpoint's `recentRelayFailures`
log. This is an environment/network-policy restriction in this run, distinct
from any of the upstream services actually being down.

- **GitHub Security Advisories** (`api.github.com/advisories`): FAILED —
  request rejected with "sessions are bound to their configured repositories;
  use repository-scoped endpoints" (this session's GitHub access is scoped to
  `hlandl/library-vuln-watch` only).
- **NVD** (`services.nvd.nist.gov`): FAILED — gateway `403` on `CONNECT`.
- **Apache Camel security page** (`camel.apache.org/security/`): FAILED —
  gateway `403` on `CONNECT`.
- **Apache Jira** (`issues.apache.org`): FAILED — gateway `403` on `CONNECT`.
- **GitHub repository sweep** (`apache/camel` releases / merged PRs / commit
  search): FAILED — out of this session's GitHub access scope (limited to the
  `library-vuln-watch` repo itself); could not be queried by any means
  available in this run.
- **github.com web UI fallback** (`github.com/apache/camel/security/advisories`,
  `github.com/apache/camel/releases`): also FAILED (`403`), so no fallback
  path existed either.

Given the sweep could not run, `state/seen.json`'s `last_run` was **left
unchanged** at `2026-07-18T04:11:09Z` (not advanced to today) so that the next
run with working network access re-sweeps the full window, including the gap
created by today's outage, and no findings from this period are silently
skipped.

## Operational note (unrelated to today's collection outage)

While preparing this run, two prior days of work were found to be missing
from `origin/main`: the 2026-07-17 and 2026-07-18 reports and their
`state/seen.json` updates had been committed on a **detached `HEAD`** in the
previous runs' checkouts rather than on the `main` branch, so they were never
actually pushed upstream despite the prior sessions' local commit history
showing them as committed. This was caught and fixed at the start of this run
by fast-forwarding `main` to include those commits and pushing — both reports
are now present in `origin/main`'s history. Future runs should double-check
`git status`/current branch before committing, in case this recurs.
