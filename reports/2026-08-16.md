# Vulnerability Watch — 2026-08-16

## Summary

✅ No new findings.

## Findings

None. No new CVE/GHSA advisories, Jira security tickets, or "silent fix" commits were identified for Apache Camel (pinned 4.8.8) in this run's window.

## Method notes

- **Lookback window:** incremental mode. `state/seen.json` `last_run` = `2026-08-15T04:06:47Z`; window swept = everything since `2026-08-13T04:06:47Z` (48h overlap per methodology step 1).
- **GHSA sweep** (`github.com/advisories?query=type:reviewed+ecosystem:maven+camel`, via WebFetch, sorted newest): 25-entry list retrieved, newest entry unchanged (`GHSA-hcm8-x79p-wx2w` / `CVE-2026-48205`, Jul 6, 2026). Every GHSA ID on the page cross-checked against `state/seen.json` — all already recorded (`AFFECTS_US` or `NOT_RELEVANT`). Nothing new.
- **GitHub repo-level advisories** (`github.com/apache/camel/security/advisories`, via WebFetch): "There aren't any published security advisories" — unchanged.
- **Commit sweep**: shallow-cloned `apache/camel` (`main`, `--shallow-since=2026-08-11`) and reviewed all 38 commits from `2026-08-13T04:06:47Z` onward, both by subject/body text and by CVE/security/vulnerability/CWE/XXE/injection/traversal/deserialization/RCE/SSRF/auth-bypass/exploit keyword grep (case-insensitive, full commit message). 5 raw hits, all verified as substring false positives on inspection:
  - `88c0d2c336e9` ("...authoritative source") — matches `RCE` inside the word "sou**rce**" (same recurring false positive noted in prior reports; not security-relevant).
  - `e5ee26c27e6e` (AWS SDK dependency bump) — matches on the dependency name `software.amazon.awssdk:**security**hub`, a routine version bump of an SDK module named "securityhub", not a fix.
  - `dccac5e0a82c`, `9e5c6ea7a73c` (camel-tui / GenAI observability work) — match on "**source** labels" / "map option names back to **source** lines" (substring hit on `RCE`).
  - `bb6882f9f2f0` — matches on "Reduces CI **resource** consumption" (substring hit on `RCE`).
  No genuine new security-relevant commit. Ticket prefixes touched in-window (CAMEL-24392, -24390, -24388, -24387, -24373, -23861) are all feature/refactor/chore/dependency work, none security-flagged.
- **Merged-PR sweep** (`mcp__github__list_pull_requests` via a temporary read-only clone context, plus `mcp__github__search_issues` global search for `security`/`CVE`/`vulnerability`/`deserialization`/`injection`/`SSRF` keywords restricted to `repo:apache/camel type:pr is:merged merged:>=2026-08-13`): keyword searches returned 0 hits; the unfiltered merged-PR listing for the window (30 PRs, #25433–#25503) was reviewed title-by-title — all routine dependency bumps, camel-tui/GenAI feature work, and Alibaba Cloud component additions. No security-relevant PR merged in-window.
- **WebSearch sweep** for new CVE/GHSA mentions surfaced only already-known IDs: `CVE-2026-49042`, `CVE-2026-27172` (alias `GHSA-5rc6-9qfp-8vwg`), `CVE-2026-33454`, `CVE-2026-46456`, `CVE-2026-46457`, `CVE-2026-46455`, `CVE-2026-53913` — all already recorded in `state/seen.json` with a verdict. One tangential hit, `GHSA-6fmv-xxpf-w3cw` (Apr 11, 2026, `apache-camel-karavan-devmode`), was reviewed and excluded: it is a Karavan devmode container image, not the `org.apache.camel` Maven group this watch tracks, and predates this run's window. `RHSA-2026:17668` was also reviewed and excluded: it bundles CVEs for Artemis/ActiveMQ, Spring Boot, Plexus-utils, and Netty in a Red Hat Camel-for-Spring-Boot bundle — none are `org.apache.camel` core CVEs, and third-party transitive dependencies are explicitly out of scope per `config/libraries.yaml`.
- No new fingerprints qualified for step 4 assessment or step 5 fix-location this run.

## Repository sync note

Confirmed clean before this run: `git fetch origin main` showed `origin/main` already at the same tip as the local checkout (`866f535`, the 2026-08-15 delivery-failure-note commit) — no sync gap.

## Source health

- **camel.apache.org** (security page): **FAILED** — `EGRESS_BLOCKED` via WebFetch; direct `curl` also rejected (`CONNECT tunnel failed`, proxy 403). Same failure mode as every run since ~2026-07-19. Substituted with the GitHub-hosted GHSA list and repo-level advisories page, both reachable via WebFetch.
- **issues.apache.org** (Jira REST API): **FAILED** — `EGRESS_BLOCKED` via WebFetch; direct `curl` rejected identically. No direct JQL sweep possible; substituted with the `git log` commit sweep and merged-PR listing above.
- **NVD** (`services.nvd.nist.gov` REST API and `nvd.nist.gov` web UI): **FAILED** — direct `curl` rejected (`CONNECT tunnel failed`, proxy 403). Substituted with WebSearch.
- **access.redhat.com** (RHSA errata cross-check): **FAILED** — direct `curl` rejected (`CONNECT tunnel failed`, proxy 403); substituted with WebSearch (see Method notes re: RHSA-2026:17668).
- **advisories.gitlab.com** (GHSA mirror cross-check): **FAILED** — direct `curl` rejected (`CONNECT tunnel failed`, proxy 403).
- **GitHub REST API** (`api.github.com`, direct `curl`): **FAILED** — 403; `apache/camel` is not an attached repository in this session (only `hlandl/library-vuln-watch` is attached), so repo-scoped MCP tools (`list_pull_requests`, etc.) are also denied for it. Anonymous git-protocol reads, GitHub web UI via WebFetch, and the GitHub MCP server's global search tools remain available and were used instead.
- **GitHub web UI via WebFetch** (`github.com/advisories?query=camel`, `github.com/apache/camel/security/advisories`, `github.com/apache/camel/releases`): **OK** — full 25-entry Maven/camel GHSA list retrieved and reconciled; no repo-level advisories or releases currently listed via WebFetch's view.
- **GitHub search API via `mcp__github__search_issues` / `search_commits`** (global, not repo-attach-gated): **OK** — 0 commit hits on `CVE`; keyword-filtered PR searches (`security`, `vulnerability`, `deserialization`, `injection`, `SSRF`) returned 0 hits; a broader unfiltered merged-PR listing for the window returned 30 PRs, all reconciled (see Method notes).
- **git protocol to github.com** (`git clone --shallow-since`): **OK** — 38 commits since 2026-08-13T04:06:47Z reviewed; nothing new.
- **WebSearch**: **OK** — used to cross-check for newly-published CVEs/GHSAs; nothing genuinely new surfaced.
- **Power Automate delivery webhook**: **FAILED** — `CONNECT tunnel failed, response 403` on the initial POST and again on the retry 30 seconds later (`curl --retry 1 --retry-delay 30 --retry-all-errors`, both attempts identical). Same failure class as the blocked sources above — the proxy rejects the CONNECT to this host outright. This continues the standing multi-week delivery outage (at least the 9th consecutive run with a failed delivery, 2026-08-08 through 2026-08-16). Report committed/pushed to the repository regardless; the all-clear status was **not** delivered to email/Teams by the automated channel this run.

**Recurring issue (multi-week, unchanged):** the four primary structured sources named in `AGENT.md` (`camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`, `api.github.com`) remain blocked by this session's network egress policy, same as every run since ~2026-07-19, and RHSA/GitLab-advisories cross-checks are blocked identically. Coverage this run relied on the GitHub web UI, a shallow `git clone` of `apache/camel`, the GitHub MCP server's global search tools, and WebSearch — this combination found no new fingerprints this run and reconciled all mentions surfaced by search against `state/seen.json`, but it remains a fallback, not a substitute for the structured sources' completeness guarantees. Standing recommendation unchanged: widen this environment's egress allowlist for `camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`, and `api.github.com`.
