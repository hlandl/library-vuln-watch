# Vulnerability Watch — 2026-07-24

**Sixth consecutive day** (2026-07-19 → 2026-07-24) that this run's execution environment
blocks outbound access to `camel.apache.org`, NVD, Apache Jira, and the delivery webhook host
— see **Source health**, unchanged in kind from the prior five reports. As on 2026-07-22/23,
the git-protocol/mirror-repo substitute channel remains reachable and was used again.

Unlike 2026-07-22 → 2026-07-23 (where widening the mirror sweep surfaced 6 and then 1 more
Camel advisories each day), **today's full re-sweep found zero new items**: the
`github/advisory-database` mirror's entire 2026 tree (`github-reviewed` + `unreviewed`,
January–July) now contains exactly the same 13 Maven `org.apache.camel` advisories already
recorded in `state/seen.json` (by CVE id or by GHSA id — same underlying findings), and no
new `apache/camel` git tag or `camel-4.8.x` maintenance-branch commit since the 2026-07-23
check. This is the first day of the outage window with a clean parity result, suggesting the
mirror's backfill of the 2026-07-06/07 disclosure batch (the source of the drift on the last
two days) has caught up.

## How today's data was collected

Direct HTTPS to `camel.apache.org`, `services.nvd.nist.gov`, `issues.apache.org`, and the
delivery webhook host all fail identically to the last five days (`CONNECT` tunnel `403`,
via both `curl` and the `WebFetch` tool — confirmed the block is not tool-specific).
`api.github.com/rate_limit` (no repo scope) succeeds; `api.github.com/repos/apache/camel`
fails `403` — the session repo-scope restriction described in the 2026-07-23 report is
unchanged (this session is scoped only to `hlandl/library-vuln-watch`).

Workarounds used, same as the last two days:

1. **`git ls-remote`** against `apache/camel` (plain git transport, not repo-scope-blocked):
   newest `camel-4.8.x` tag is still **`camel-4.8.9`**; the `camel-4.8.x` maintenance branch
   head is still `5a31b932322777b8eb5f091cf9c6f016a62fe896` — both unchanged from 2026-07-23.
2. **Sparse, blob-filtered clone of `github/advisory-database`** (`--filter=blob:none`,
   sparse-checkout of `advisories/{github-reviewed,unreviewed}/2026`) — 8,406 +
   34,191 advisory JSON files checked out; 53 mention "camel" (case-insensitive) anywhere in
   the record. Of those, 13 are genuine `Maven` / `org.apache.camel:*` advisories — all 13
   already present in `state/seen.json`. The remaining 40 are false positives from the
   keyword sweep, not Apache Camel:
   - `CVE-2026-45760` (Apache **Camel K**, Go module `github.com/apache/camel-k/v2` — different
     project, already recorded NOT RELEVANT/out-of-scope).
   - 33 of the 2026-07-06/07 batch of unreviewed CVEs (`CVE-2026-42527`, `-49365`, `-56139`,
     `-49098`, `-49086`, `-46592`, `-46588`, `-46587`, `-46454`, `-53913`, `-40859`, `-55994`,
     `-46456`, `-40047`, `-49099`, `-46455`, `-48205`, `-48203`, `-46726`, `-48204`, `-46584`,
     `-48206`, `-46457`, `-43867`, `-55993`, `-46590`, `-43866`, `-46453`, `-56140`, `-49097`,
     `-43865`, `-46585`, `-46591`, `-49042`, `-40860`) — every one already in `state/seen.json`
     from the 2026-07-22/23 runs.
   - `CVE-2026-39842` (OpenRemote, `io.openremote:openremote-manager` — unrelated Maven
     package, "camel" appears only incidentally; already recorded NOT RELEVANT).
   - `CVE-2026-47668` / `CVE-2026-48017` (npm `dbgate-serve` / `dbgate-api` — DbGate script
     runner RCEs, unrelated product; keyword match is coincidental, not Apache Camel).
   - `CVE-2026-43421` (Linux kernel USB gadget `f_ncm` net_device fix — "camel" appears only
     as part of an unrelated commit-message email address, `...@crapouillou.net`, itself
     referencing the GNOME/Evolution "Camel" mail library, not Apache Camel).

   None of the last three are `org.apache.camel` Maven packages, so per `config/libraries.yaml`'s
   `maven_group: org.apache.camel` filter they are correctly excluded before ever becoming
   candidate findings — not recorded as processed fingerprints in `state/seen.json`.
3. **`raw.githubusercontent.com`** — used to re-confirm the pinned-version (4.8.8) file state
   for spot checks; no new backport work needed since no new AFFECTS US finding surfaced.

This still does not substitute for the Apache Jira sweep (no *SUSPECTED*, no-CVE-yet findings
could be surfaced) or the GitHub PR/commit keyword search for silent hardening fixes (Search
API is repo-scope-blocked). `last_run` in `state/seen.json` is **left unchanged** at
`2026-07-18T04:11:09Z`, for the same reason as the last three reports: it should reflect the
last run that actually covered Jira and the PR/commit search, so that whichever run regains
access re-sweeps the full outage window for those two sources rather than just from today.

## Summary

✅ No new findings — full parity between today's git-protocol/mirror sweep and the findings
already recorded in `state/seen.json`. (Two pre-existing items remain open from earlier runs
and are unchanged: `CVE-2026-42527` and `CAMEL-23522`, both `NEEDS_REVIEW`; see prior reports
for detail — this run did not re-investigate them, per the methodology's dedup rule.)

## Findings

None new today.

## Apache Camel release / silent-fix check (git protocol)

- `git ls-remote --tags` against `apache/camel`: newest `camel-4.8.x` tag is still
  **`camel-4.8.9`** — unchanged from 2026-07-22 and 2026-07-23, still more than 180 days old.
- `camel-4.8.x` maintenance branch head: `5a31b932322777b8eb5f091cf9c6f016a62fe896` — identical
  to the hash recorded in the 2026-07-23 report; no new commits merged.
- Could not perform the GitHub PR/commit keyword search (`is:merged CVE`/`security`/
  `vulnerability`/`CWE` against `apache/camel`) — Search API is repo-scope-blocked. This
  remains the main coverage gap versus a normal run, unchanged from the last five days.

## Source health

- **Apache Camel security page** (`camel.apache.org/security/`): **FAILED** — egress-policy
  block (`CONNECT` `403`, confirmed via both `curl` and `WebFetch`), 6th consecutive day.
- **NVD** (`services.nvd.nist.gov`): **FAILED** — same block, 6th consecutive day.
- **Apache Jira** (`issues.apache.org`): **FAILED** — same block (confirmed via both `curl`
  and `WebFetch`), 6th consecutive day. No Jira-only *SUSPECTED* findings could be surfaced
  this run.
- **GitHub API** (`api.github.com` — GHSA-by-ecosystem listing, releases, PR/commit search):
  **FAILED** — session repo-scope restriction, unchanged from 2026-07-23; `apache/camel` is
  not an authorized repo for this session.
- **GitHub web UI** (`github.com/apache/camel/*`): **FAILED** — same restriction.
- **`raw.githubusercontent.com`**: **OK** for known paths.
- **`apache/camel` git-protocol transport**: **OK** — used for tag/branch discovery.
- **`github/advisory-database` git-protocol clone**: **OK** — full 2026 tree re-swept, clean
  parity result today (no new Camel-relevant advisories since 2026-07-23).
- **Delivery webhook**: see **Delivery** below.

## Delivery

**Delivery failed.** Both attempts (initial + retry after 30s) to POST this report to the
configured delivery webhook were blocked by the same blanket egress-policy `403` described
above (`curl` exit 56, "CONNECT tunnel failed, response 403") — the webhook host could not be
reached at the network level, not merely rejected by the endpoint itself. No email or Teams
notification could be sent for 2026-07-24 via the normal channel; the on-call is being
notified out-of-band via a push notification from this session instead, and this report plus
the push to `origin/main` are the only record of today's run until delivery is retried. This
is the sixth consecutive day of delivery failure.

## Recurring issue — needs owner attention

Unchanged in substance from the prior five reports: this scheduled routine has now gone six
consecutive days (2026-07-19 → 2026-07-24) unable to reach `camel.apache.org`, NVD, Jira, or
the delivery webhook directly. Today's clean parity result (zero new relevant Camel advisories
found by the git-protocol/mirror substitute, versus the 6- and 1-advisory drift on the prior
two days) shows the substitute channel is not itself the problem today — but Jira and the
GitHub PR/commit search remain completely uncovered for the whole outage window, which is a
real, accumulating coverage gap for *SUSPECTED* (no-CVE-yet) findings regardless of how well
the mirror happens to be caught up on any given day. Recommended actions, unchanged:

1. Review and widen this environment's outbound egress allowlist to include at minimum:
   `camel.apache.org`, `services.nvd.nist.gov`, `issues.apache.org`, and the Power Automate
   delivery webhook host.
2. Grant this session's GitHub scope access to `apache/camel` (via `add_repo`, with explicit
   owner sign-off) so the GitHub API-based sources (GHSA-by-ecosystem listing, release list,
   PR/commit search) are usable directly instead of via the improvised git-protocol/mirror-repo
   substitute.
