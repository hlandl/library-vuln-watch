# Vulnerability Watch — 2026-08-13

## Summary
| Library | Finding | Severity | Verdict | Official fix |
|---|---|---|---|---|
| Apache Camel | CAMEL-23588 — `camel-undertow` `UndertowHeaderFilterStrategy` websocket-prefix filtering | LOW *(estimated)* | NOT RELEVANT (component not used) | Not needed — see below |

## Findings

### CAMEL-23588 — camel-undertow: UndertowHeaderFilterStrategy extended to filter legacy websocket.* header prefix

- **Is this a vulnerability?** `SUSPECTED`. No CVE/GHSA has been assigned. Surfaced indirectly via the GitHub repo "silent fix" sweep: commit [`7226ce876`](https://github.com/apache/camel/commit/7226ce876b1432e27e390516401b287501816ddc) (CAMEL-24360, 2026-08-10, docs-only) clarifies that `CAMEL-23588`'s header-filtering hardening — extending `UndertowHeaderFilterStrategy` to also strip the legacy `websocket.*` Exchange-header prefix (in addition to `Camel*`/`camel*`/`org.apache.camel.*`) — is the same control-header-filtering class of fix already tracked under `CAMEL-23522` (`state/seen.json`, recorded 2026-07-15, verdict `NEEDS_REVIEW`) and shares its PR (`#23522`). `CAMEL-23588` itself was not previously recorded as a distinct fingerprint.
- **Does the affected range include our pinned version (4.8.8)?** `UNCLEAR` — not independently re-verified against the 4.8.8 tag this run; moot given the component check below.
- **Is an affected component in `used_components`?** `NOT-USED`. `camel-undertow` is not present in `config/libraries.yaml`'s `used_components` list.
- **Severity:** LOW *(estimated — header-filter hardening / defence-in-depth, gated by component non-use; the related wiring-bypass bug, `CAMEL-24360`, was already assessed and recorded 2026-08-08)*.
- **Verdict:** **NOT RELEVANT** (component not used).
- **Sources:** [Commit 7226ce876](https://github.com/apache/camel/commit/7226ce876b1432e27e390516401b287501816ddc) · related: `CAMEL-23522` (already in `state/seen.json`) · `CAMEL-24360` (already in `state/seen.json`).
- **Official fix / backport assessment:** Not performed — component not in use, per methodology step 5's scope.

## Notes (dedup reconciliation — no new finding)

- Commit search cross-check (`mcp__github__search_commits`, `repo:apache/camel (CVE OR security OR vulnerability OR CWE) committer-date:>=2026-08-09`) returned exactly one hit: `650dd2d76` (`chore(deps): Bump jetty-version from 12.1.11 to 12.1.12`) — already recorded 2026-08-11, no new commit.
- Merged-PR cross-check (`mcp__github__search_issues`, `is:pr is:merged (CVE OR security OR vulnerability) updated:>=2026-08-09`) returned 19 hits: routine dependency bumps, new/unrelated feature components (`camel-alibaba`, `camel-ubl`, `camel-rest-postman`, AI-tool work), `CAMEL-24371` (already recorded 2026-08-12), and `#25443` "Pin GitHub actions" — CI supply-chain hardening for this repository's own build pipeline, not a change to a shipped Camel artifact, so it isn't a candidate finding for this watch.
- `GHSA-hfg5-xpvw-c9x4` (surfaced via web search while probing for anything new) resolves to `CVE-2020-11971`, already in `state/seen.json` (`NOT_RELEVANT`) — no new finding.
- Apache Camel 4.22.0 LTS shipped 2026-08-11 (`4.14 is EOL` as of the same date, per commit `3ec2d2c8a`). Its upgrade guide (`camel-4x-upgrade-guide-4_22.adoc`) was reviewed directly from the cloned repo for CVE/security mentions — none beyond the already-known JAAS/security-config text unrelated to this window.
- `CAMEL-24371` (`camel-a2a` `WebhookUrlValidator`) and `CAMEL-24379` (`camel-as2` `HostnameVerifier`) reappeared in this run's commit window but are already recorded 2026-08-12 (`NOT_RELEVANT`) — deduped, not re-reported.

## Delivery

- Power Automate webhook: **delivery failed.** `CONNECT tunnel failed` on the initial POST and again on the retry 30 seconds later — the webhook host is blocked by this session's network egress policy, same failure class as the sources below. Report committed/pushed to the repository regardless; the all-clear status above was **not** delivered to email/Teams by the automated channel this run.
- **This is the 6th consecutive run with a failed delivery** (2026-08-08 through 2026-08-13) — a standing outage, not a blip.

## Source health

- **camel.apache.org** (security page): **FAILED** — `EGRESS_BLOCKED` via WebFetch (`Access to camel.apache.org is blocked by the network egress proxy`); direct `curl` gets no response (proxy status endpoint logs `connect_rejected`, gateway 403 to CONNECT). Same failure mode as every run since ~2026-07-19.
- **issues.apache.org** (Jira REST API): **FAILED** — `EGRESS_BLOCKED` via WebFetch; direct `curl` rejected identically (proxy status: `connect_rejected`, gateway 403). No direct JQL sweep possible; substituted with the `git log` commit sweep below.
- **NVD** (`services.nvd.nist.gov`): **FAILED** — `EGRESS_BLOCKED` via WebFetch; direct `curl` rejected identically (proxy status: `connect_rejected`, gateway 403).
- **GitHub REST API** (`api.github.com` — advisories/releases, direct `curl`): **FAILED** — 403 ("GitHub access to this repository is not enabled for this session" / "sessions are bound to their configured repositories"); `apache/camel` is not an attached repository (only anonymous git-protocol reads and the GitHub MCP server's global search tools are available for it in this session, see below).
- **GitHub web UI via WebFetch** (`github.com/apache/camel/security/advisories`, `/releases`): **OK** — repo-level security-advisories page confirmed empty ("no published security advisories" — consistent with prior runs; Camel publishes its own CVEs via `camel.apache.org/security/`, not GHSA-native for this repo); no GitHub Releases exist for `apache/camel` (consistent with prior runs).
- **GitHub search API via `mcp__github__search_commits`/`search_issues`** (global, not repo-attach-gated): **OK** — see Notes above; 1 commit hit and 19 PR hits, all reconciled (already-seen or non-security).
- **`mcp__github__list_releases`** for `apache/camel`: **FAILED** — "repository not configured for this session" (this specific tool requires the repo attached, unlike `search_commits`/`search_issues`). No gap: the web-UI check above already confirmed no releases exist.
- **git protocol to github.com** (`git clone`): **OK** — `--shallow-since=2026-08-08` clone of `apache/camel` (`main`); `git log --since=2026-08-08` (82 commits) reviewed by subject (CVE/security/vulnerability/CWE/deserialization/injection/traversal/RCE keyword sweep) and by diff where security-adjacent. Surfaced the one new finding above (`CAMEL-23588`); nothing else security-relevant in the window.
- **WebSearch**: **OK** — used to cross-check for newly-published CVEs/GHSAs in the window and to research `CAMEL-23588`/`GHSA-hfg5-xpvw-c9x4`; nothing genuinely new surfaced beyond what's recorded above.
- **Power Automate delivery webhook**: **FAILED** — egress-policy block (`CONNECT tunnel failed`), same class as the sources above; see Delivery section. Retried once after 30s per the runbook, failed identically both times. **6th consecutive day of delivery failure.**

**Recurring issue (multi-week, unchanged):** the four primary structured sources named in `AGENT.md` (`camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`, `api.github.com`) remain blocked by this session's network egress policy, same as every run since ~2026-07-19. Coverage this run relied on the GitHub web UI (via WebFetch), a shallow `git clone` of `apache/camel`, the GitHub MCP server's global search tools, and WebSearch — this combination caught today's one new fingerprint and reconciled it correctly, but it is a fallback, not a substitute for the structured sources' completeness guarantees. Standing recommendation unchanged: widen this environment's egress allowlist for `camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`, and `api.github.com`; also widen the Power Automate delivery webhook host.
