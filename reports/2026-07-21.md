# Vulnerability Watch — 2026-07-21

## Summary

⚠️ **No sources could be swept today.** This is the **third consecutive day**
(2026-07-19, 2026-07-20, 2026-07-21) that this run's execution environment has
blocked outbound network access to essentially every configured data source
(see Source health below). This is **not** a clean bill of health — it means
the sweep did not happen, not that no vulnerabilities exist. No new findings
were collected or assessed for Apache Camel 4.8.8 (or any other configured
library).

| Library | Finding | Severity | Verdict | Official fix |
|---|---|---|---|---|
| — | — | — | **COLLECTION FAILED — see Source health** | — |

## Findings

None collected. No candidate data could be retrieved from any authoritative
source, so no assessment (vulnerability confirmation, affected-version check,
component-use check, severity) could be performed.

## Source health

Two distinct failure modes were observed, both confirmed via
`curl -sS "$HTTPS_PROXY/__agentproxy/status"` (`recentRelayFailures`) and by
direct `curl`/WebFetch attempts:

- **Blanket egress-policy block** (`connect_rejected`, gateway `403` on
  `CONNECT`) — every non-GitHub host tried was rejected identically,
  including a neutral sanity host (`example.com`):
  - **Apache Camel security page** (`camel.apache.org/security/`): FAILED.
  - **NVD** (`services.nvd.nist.gov`): FAILED.
  - **Apache Jira** (`issues.apache.org`): FAILED.
  - **Delivery webhook**: FAILED (see Delivery below).
- **GitHub session-scope restriction** (application-level `403`, connection
  itself succeeds) — this session's GitHub access is bound to
  `hlandl/library-vuln-watch` only; `apache/camel` is out of scope:
  - `api.github.com` (GHSA advisories, releases, PR/commit search): FAILED —
    `"GitHub access to this repository is not enabled for this session."`
  - `github.com/apache/camel/*` (web UI, `.atom` feeds): FAILED, same reason.
  - `codeload.github.com` (tarball download): FAILED, same reason.
  - **Partial exception**: `raw.githubusercontent.com` **is** reachable and
    is not subject to the repo-scope check (verified: known files under
    `apache/camel@main` returned `200`). However this host only serves exact,
    already-known paths — it has no directory listing or search, so it cannot
    be used to *discover* new commits, releases, or advisories. It could not
    yield any findings this run and does not meaningfully substitute for the
    blocked sources.

Given the sweep could not run, `state/seen.json`'s `last_run` is **left
unchanged** at `2026-07-18T04:11:09Z` (not advanced) so that the next run with
working network access re-sweeps the full window, including the gap created
by this outage and the two preceding it, and no findings from this period are
silently skipped.

## Delivery

**Delivery failed.** Both attempts (initial + retry after 30s) to POST this
report to the configured delivery webhook were blocked by the same blanket
egress-policy `403` described above (`curl` exit 56, "CONNECT tunnel failed,
response 403") — the webhook host could not be reached at the network level,
not merely rejected by the endpoint itself. No email or Teams notification
could be sent for 2026-07-21 via the normal channel; the on-call is being
notified out-of-band via a push notification from this session instead.

## Recurring issue — needs owner attention

This is the **third consecutive day** (2026-07-19, 2026-07-20, 2026-07-21)
that this scheduled routine has been unable to reach any upstream source or
the delivery webhook. The root cause is an environment-level outbound
network/egress policy (the agent proxy gateway), not an upstream outage —
confirmed by the identical `403` rejection of a neutral sanity host
(`example.com`) alongside every real source. Additionally, this session's
GitHub access is scoped to this repository only, so even GitHub-hosted
sources for `apache/camel` (advisories, releases, commit search) are
independently blocked regardless of the network policy.

If this network policy is not adjusted, the watch cannot function going
forward: it will keep re-arming the same (and growing) lookback window
without ever collecting real data, and genuine new vulnerabilities in Apache
Camel 4.8.8 could go unnoticed for as long as the outage persists. Recommended
actions for the owner:

1. Review and widen this environment's outbound egress allowlist to include
   at minimum: `camel.apache.org`, `services.nvd.nist.gov`,
   `issues.apache.org`, `api.github.com`, `github.com`, and the Power
   Automate delivery webhook host.
2. Separately, consider granting this session's GitHub scope access to
   `apache/camel` (via `add_repo`, with explicit owner sign-off) so the
   GitHub-hosted sources are usable even if the broader network policy stays
   restrictive.
