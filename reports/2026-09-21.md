# Vulnerability Watch — 2026-09-21

## Summary

| Library | Finding | Severity | Verdict | Official fix |
|---|---|---|---|---|
| apache-camel | ✅ No new findings | n/a | n/a | n/a |

**0 new findings this run.** Every candidate identifier surfaced during this run's sweep resolved to a fingerprint already present in `state/seen.json` from a prior run, with no verdict change. The three carried-over `NEEDS_REVIEW` items (`CAMEL-24296`, `CAMEL-24298`, `CAMEL-24299`) remain open, unchanged.

## Findings

None new. Candidates checked and confirmed already-tracked (no action, no state change):

| Identifier | Disposition |
|---|---|
| CVE-2026-53913 | Already seen 2026-07-23, `NOT_RELEVANT` |
| GHSA-5rc6-9qfp-8vwg (CVE-2026-27172, camel-consul) | Already seen 2026-07-15, `NOT_RELEVANT` |
| CVE-2026-40860 | Already seen 2026-07-23, `AFFECTS_US` (no new info) |
| CVE-2026-33454 | Already seen 2026-07-15, `AFFECTS_US` (no new info) |
| CVE-2026-33453 (alias GHSA-695c-x5gc-94gj) | Already seen 2026-07-15, `NOT_RELEVANT` |
| CVE-2026-46590 (camel-pqc) | Already seen (2026-07-06 batch), `NOT_RELEVANT` — camel-pqc not a `used_component` |
| CVE-2026-42527 (permissive default ObjectInputFilter) | Already seen 2026-07-22, `NOT_RELEVANT` — pinned 4.8.8 predates the ObjectInputFilter mechanism (CAMEL-24296) |
| CVE-2026-48206 (camel-jira) | Already seen 2026-07-22, `NOT_RELEVANT` |
| CAMEL-24632 (camel-http proxy-scheme default fix, PRs #26167 / backport #26338) | Already seen 2026-09-08, `NOT_RELEVANT` — configuration-default correctness fix, not a vulnerability; camel-http not a `used_component` |
| GHSA-4jrw-64vr-7g8m / CVE-2025-66169 (camel-neo4j cypher injection) | Already seen 2026-07-15, `NOT_RELEVANT` — camel-neo4j not a `used_component` |
| CVE-2026-80351 / -80352 / -80354 (Camel K, published 2026-09-08) | Already seen, `NOT_RELEVANT` — separate Camel K operator deployable, not `org.apache.camel` core |

Filtered as noise (not an Apache Camel finding, not persisted): **GHSA-82x6-q7mm-w9cf** — a call-stack-overflow advisory in the npm `toml` package, unrelated to `org.apache.camel`; surfaced only as an incidental search hit.

### Carry-over check

`CAMEL-24296`, `CAMEL-24298`, `CAMEL-24299` (all `NEEDS_REVIEW` since 2026-08-04/06/07): targeted searches for each ticket found only their resolution details (all shipped as hardening features in Camel 4.22.0 — CAMEL-24296: shared deserialization-filter helper; CAMEL-24298: allow-list for dynamic-URI EIPs; CAMEL-24299: `SUPPORT_DTD=false` in `XmlStreamDetector`). No CVE/GHSA has been assigned to any of the three. **No verdict change** — all three carried forward as-is pending a future CVE assignment or a closer manual read against the pinned 4.8.8 baseline.

## Method notes

- **Lookback window:** incremental mode. `state/seen.json` `last_run` (start of this run) = `2026-09-20T04:10:54Z`; window swept = everything since `2026-09-18T04:10:54Z` (48h overlap per methodology step 1).
- Direct access to `camel.apache.org`, `issues.apache.org`, `services.nvd.nist.gov`, and `api.github.com` remains blocked for this session (see Source health) — sourcing relied entirely on `WebSearch` snippets, cross-checked individually against `state/seen.json` by identifier. `github.com` unauthenticated web pages, which substituted successfully in the 2026-09-20 run, returned HTTP 403 this run (see Source health) and were not usable.

## Source health

- **camel.apache.org, issues.apache.org, services.nvd.nist.gov**: **FAILED** — egress proxy rejects the CONNECT (`403`, confirmed via both `curl` and `WebFetch`, `EGRESS_BLOCKED` for camel.apache.org). Same failure mode reported in every run since ~2026-07-19 (now 10+ weeks).
- **api.github.com**: **FAILED** — `403`; `apache/camel` is not an attached repository in this session's GitHub API gateway (scoped to `hlandl/library-vuln-watch` only).
- **github.com (unauthenticated web pages)**: **FAILED** this run — `curl` and `WebFetch` both returned `403` for `github.com/apache/camel/releases` and advisory/PR pages (worked as of 2026-09-20; now also blocked). Could not be used as a substitute this run.
- **WebSearch**: **OK** — used as the sole source this run; every surfaced identifier was individually verified against `state/seen.json`.
- **`git push`**: pending — see commit below.
- **Power Automate delivery webhook**: pending at time of writing — see delivery notes appended after the POST attempt.

## Delivery

**0 new findings.** No `AFFECTS US` or `NEEDS REVIEW` items were newly raised this run. The three carried-over `NEEDS_REVIEW` items remain open, unchanged, with no new information.

⚠ **Sourcing has now degraded further**: in addition to the 10-week outage on `camel.apache.org`/`issues.apache.org`/NVD/`api.github.com`, the `github.com` unauthenticated web-page fallback that covered the GHSA/PR sweep in the 2026-09-20 run is now also returning 403. `WebSearch` remains the only working source; a human should verify that no significant Camel security event is being missed while all direct sources stay dark, and check the egress proxy allowlist / GitHub App repository scope.
