# Vulnerability Watch — 2026-07-20

## Summary

⚠️ **No sources could be swept today.** This run's execution environment blocked
outbound network access to every configured data source (see Source health
below) — this is the **second consecutive day** this has happened (see
2026-07-19.md for yesterday's identical outage). This is **not** a clean bill
of health — it means the sweep did not happen at all, not that no
vulnerabilities exist. No new findings were collected or assessed for Apache
Camel 4.8.8 (or any other configured library).

| Library | Finding | Severity | Verdict | Official fix |
|---|---|---|---|---|
| — | — | — | **COLLECTION FAILED — see Source health** | — |

## Findings

None collected. No candidate data was retrieved from any source, so no
assessment (vulnerability confirmation, affected-version check, component-use
check, severity) could be performed.

## Source health

All sources failed identically: the proxy gateway in this run's environment
rejected the outbound `CONNECT` for every external host with `403` ("policy
denial or upstream failure"), confirmed via the agent-proxy status endpoint's
`recentRelayFailures` log. A bare sanity check against `example.com` (a host
with no special significance to this project) failed the same way, showing
this is a blanket network-policy block in this run's environment, not
something specific to security-advisory hosts.

- **GitHub Security Advisories** (`api.github.com/advisories`): FAILED —
  "This GitHub API path is not available: sessions are bound to their
  configured repositories. Use repository-scoped endpoints." This session's
  GitHub access is scoped to `hlandl/library-vuln-watch` only.
- **NVD** (`services.nvd.nist.gov`): FAILED — gateway `403` on `CONNECT`.
- **Apache Camel security page** (`camel.apache.org/security/`): FAILED —
  `403` via both direct HTTPS and the WebFetch tool.
- **Apache Jira** (`issues.apache.org`): FAILED — `403` via both direct HTTPS
  and WebFetch.
- **GitHub repository sweep** (`apache/camel` releases / merged PRs / commit
  search): FAILED — confirmed via the GitHub MCP tool directly: "Access
  denied: repository apache/camel is not configured for this session.
  Allowed repositories: hlandl/library-vuln-watch." Out of this session's
  GitHub access scope; could not be queried by any means available in this
  run (raw API, WebFetch, or GitHub MCP tools all denied).
- **github.com web UI fallback** (`github.com/apache/camel/security/advisories`,
  `github.com/apache/camel/releases`): also FAILED (`403`), so no fallback
  path existed either.
- **Delivery webhook**: FAILED — see Delivery section below; same gateway
  `403` policy denial.

Given the sweep could not run, `state/seen.json`'s `last_run` is **left
unchanged** at `2026-07-18T04:11:09Z` (not advanced) so that the next run
with working network access re-sweeps the full window, including the gap
created by both this outage and yesterday's, and no findings from this period
are silently skipped.

## Delivery

**Delivery failed.** Both attempts (initial + retry after 30s) to POST this
report to the configured delivery webhook were blocked by the same
environment network policy that blocked the source sweep above — the proxy
gateway rejected the outbound `CONNECT` to the webhook host with a `403`
policy denial both times (`curl` exit 56, "CONNECT tunnel failed, response
403"). No email or Teams notification could be sent for 2026-07-20 via the
normal channel; the on-call is being notified out-of-band instead (push
notification from this session).

## Recurring issue — needs owner attention

This is the **second day in a row** (2026-07-19, 2026-07-20) that this
scheduled routine has been completely unable to reach any upstream source or
the delivery webhook, due to what appears to be an environment-level network
policy (the agent proxy gateway) rather than an upstream outage. Every
external host tried — including a neutral sanity host — was rejected at the
`CONNECT` stage. If this network policy is not adjusted for this environment,
the watch cannot function at all going forward: it will keep re-arming the
same lookback window without ever collecting real data. Recommend checking
the remote-execution environment's outbound network policy/allowlist for
this session.
