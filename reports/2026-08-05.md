# Vulnerability Watch — 2026-08-05

## Summary

**✅ No new `AFFECTS US` or `NEEDS REVIEW` findings today.** 26 new distinct changes on
`apache/camel` `main` (27 commits; two commits share the `CAMEL-24310` Jira key) were reviewed
and ruled `NOT RELEVANT` — either the touched artifact is not in `used_components`, the change is
a non-security bug fix/dev-tooling/docs change, or it is a third-party transitive dependency bump
out of this watch's scope. A full re-sweep of the `github/advisory-database` GHSA mirror (2026
tree, both `github-reviewed` and `unreviewed`) found **zero new** Camel-related advisories — every
one of the 22 (package-scoped) + 22 (text-matched, CVE-only) Camel-related entries found there
already resolves to a fingerprint in `state/seen.json`.

| Library | Finding | Severity | Verdict | Official fix |
|---|---|---|---|---|
| apache-camel | 26 new distinct changes reviewed (TUI/jbang features, `camel-mcp-server` and `camel-google-secret-manager` work, an aws2 null-guard, a RouteService NPE fix, a SEDA `CountDownLatch` fix, dependency bumps, docs) | — | NOT RELEVANT | n/a — see rationale below |

Reference (not a new finding, tracked since 2026-07-31, unchanged today): **CAMEL-24293** (Zip/Tar
Slip in `camel-zipfile`, `AFFECTS US`) — official fix (`apache/camel@cae63dd7a3a4`, PR #25218) is
on `main` but **still not backported to `camel-4.8.x`**, re-confirmed directly today (see
"Outstanding items" below).

## How today's data was collected

Direct HTTPS to `camel.apache.org/security/`, `services.nvd.nist.gov`, `issues.apache.org`, and
the delivery webhook host all failed identically via both `curl` and `WebFetch`
(`CONNECT tunnel failed, response 403` / HTTP 403) — independently re-verified this run, not
assumed from prior reports. `api.github.com` (GHSA-by-ecosystem listing, releases API,
PR/commit search scoped to `apache/camel`) also returned `403`.

Substitute channels used, all working today:

1. **Anonymous git-protocol clone of `apache/camel`** (`GIT_LFS_SKIP_SMUDGE=1 git clone
   --shallow-since=2026-07-20`) — this session's git proxy serves anonymous reads of public
   GitHub repos even though `apache/camel` is not an attached repo. Used for the `main`-branch
   commit sweep, `camel-4.8.x` maintenance-branch fetch, and tag/release discovery.
2. **`main`-branch commit sweep**: 71 commits since 2026-08-02 (48h+ overlap over the last
   report's actual cutoff). Deduped against `state/seen.json` by Jira key (`CAMEL-\d+` in the
   subject) falling back to `apache/camel@<sha>` — this correctly dropped the 45 commits already
   recorded from the 2026-08-04 run (which used the same Jira-key fingerprint, not the raw SHA).
   26 distinct new fingerprints remained (27 commits) and were individually reviewed (message +
   diff) against `used_components` and for security relevance.
3. **`camel-4.8.x` maintenance branch**: fetched directly; tip is still
   `5a31b932322777b8eb5f091cf9c6f016a62fe896` (2025-09-17), unchanged since the last check — no
   backport activity. Confirmed via `git show FETCH_HEAD:...ZipFileDataFormat.java` that line 109
   still reads `entry.getName()` unstripped, and `git merge-base --is-ancestor` confirms the
   `CAMEL-24293` fix commit is not an ancestor of `camel-4.8.x`.
4. **`github/advisory-database` sparse git clone** (`--filter=blob:none --sparse`, cone-checkout
   of `advisories/github-reviewed/2026` + `advisories/unreviewed/2026`, ~45.8k advisory files) —
   completed without truncation this run (unlike 2026-08-04's partial sweep). Searched both by
   structured `affected[].package.name` (`org.apache.camel:*`) and by a broader case-insensitive
   text match on "camel" to also catch `unreviewed` entries that don't yet have parsed `affected`
   data. Found 44 Camel-related advisories (2 duplicates across the two search methods removed);
   all 44 resolve to a CVE/GHSA fingerprint already in `state/seen.json`. One text match
   (`GHSA-w77g-4c35-prwp`) was a false positive (Linux kernel USB-gadget advisory, unrelated word
   collision) and was discarded.
5. **`raw.githubusercontent.com`**: reachable (spot-checked).

`last_run` in `state/seen.json` has been **updated to today's run time**, resuming standard
incremental tracking (previous runs left it frozen at `2026-07-18T04:11:09Z` to force a wide
re-sweep once Jira/NVD/the security page recovered; since the outage is a pure network-egress
block unrelated to the lookback window, freezing it no longer serves that purpose, and the
git-based commit/GHSA-mirror sweeps have been providing continuous coverage independent of this
field for weeks regardless).

## Findings

No new `AFFECTS US` or `NEEDS REVIEW` findings today. All 26 new distinct changes were ruled
`NOT RELEVANT` — see rationale below. None required an upstream-fix/backport write-up under
step 5 of the methodology.

## New changes reviewed and ruled out this run

- **`camel-jbang` TUI features** (9 commits: web indicator repositioning, open-from-directory,
  F2/F10 popups, YAML validate-on-save, EIP tab completion, F1 help text, component/endpoint tab
  completion, YAML DSL parameter tab completion) — `camel-jbang` is **not** in `used_components`.
  Dev-tooling features, not security fixes.
- **CAMEL-24202** — `camel-tui` gains a `--web` flag to serve the TUI over a browser; also updates
  `security-model.adoc`. `camel-jbang`/`camel-tui` not in `used_components`.
- **CAMEL-24341** — `camel-google-secret-manager`: fixes GCP vault-refresh `PubsubReloadTriggerTask`
  defects. **`camel-google-secret-manager` is not in `used_components`** — out of scope.
- **CAMEL-24353, CAMEL-24310, CAMEL-24311** — `camel-mcp-server` work: selectable target server
  type, a new test proving the main HTTP server's `camel.server.authentication*` handlers already
  protect the `/mcp` endpoint (test-only, confirms no vulnerability rather than fixing one), and
  new `camel.server.mcp-*` config/autowiring. **`camel-mcp-server` is not in `used_components`**
  — out of scope regardless.
- **CAMEL-24262** — `camel-aws2-bedrock*`/`camel-aws-common`: guards a nullable
  `awsErrorDetails()` call when logging AWS error codes (defensive null-check, prevents a
  secondary NPE while logging). **No `camel-aws*` artifact is in `used_components`** — out of
  scope; also not a trust-boundary fix.
- **CAMEL-24340** — `camel-salesforce` option/header documentation improvements only.
  `camel-salesforce` not in `used_components`.
- **docs(jms)** (`2ce75e8f6ddd`) — `camel-jms` doc-only update (Jakarta API reference cleanup, no
  code change). `camel-jms` **is** in `used_components` but this touches no runtime code.
- **CAMEL-24301** — `RouteService.warmUp()`/`setUp()` (`core/camel-base-engine`, used) threw a
  secondary NPE instead of `FailedToStartRouteException` when the root-cause exception had no
  message (e.g. a bare `NullPointerException`). Reviewed the diff: purely a message-extraction
  helper walking the cause chain, no trust-boundary or input-validation change — an availability/
  diagnostics fix, not a vulnerability.
- **CAMEL-24311, CAMEL-24310 (bridge/SPI)** — further `camel-mcp-server` scaffolding/config
  metadata. Not in `used_components`.
- **CAMEL-23952** — `camel-langchain4j-agent`: adds `executeToolsConcurrently` option. Not in
  `used_components`.
- **CAMEL-23129** — `camel-seda` (`ThreadPerTaskSedaConsumer`, used): fixes a `CountDownLatch`
  count mismatch that made `prepareShutdown()` wait the full shutdown timeout instead of
  completing promptly. Reviewed the diff: pure concurrency/lifecycle bug fix (latch count 1 vs.
  `concurrentConsumers`), no attacker-influenced input or trust boundary involved. Not security.
- **Dependency bumps** (`hapi-fhir-core-version` 6.9.12→6.10.0, `aws-java-sdk2-version`
  2.50.1→2.50.2) — third-party dependency bumps, out of this watch's scope per
  `config/libraries.yaml`.
- **CAMEL-24336** — `camel-main` catalog/metadata: adds enum-type detection for `TimeUnit`/
  `LeaseResourceType` options in generated tooling JSON. Metadata/tooling only, no runtime
  behavior change.
- **CAMEL-24309** — `camel-ai-tool`: adds an `AiToolRegistryListener` SPI for tool-registration
  change notifications. Not in `used_components`.
- **chore(tui)** (`d7f3713f`) — further `camel-jbang` TUI tab-completion work. Not in
  `used_components`.
- **CAMEL-24335** — updates the Artemis test-dependency groupId (`org.apache.activemq.artemis` →
  `org.apache.artemis`) in `test-infra`/`camel-itest` poms. Third-party/test-infra dependency
  metadata, out of scope.
- **CAMEL-24330** — adds an AI/LLM integration guide and expands `openai`/`langchain4j-chat`
  component docs. Documentation only.

## Outstanding items (carried forward, not new findings)

- **CAMEL-24293** (Zip/Tar Slip path traversal, `camel-zipfile`, `AFFECTS US`, first reported
  2026-07-31): the upstream fix (`apache/camel@cae63dd7a3a4`, PR #25218) remains on `main` only.
  Re-verified today: `camel-4.8.x`'s `ZipFileDataFormat.java` line 109 still sets
  `Exchange.FILE_NAME`/`CamelFileName` from the raw, unstripped zip entry name, and
  `git merge-base --is-ancestor cae63dd7a3a4... camel-4.8.x` still returns false. See the
  2026-08-04 report for the full backport recipe (wrap `entry.getName()` with the already-present
  `FileUtil.stripPath()` at the two call sites in `ZipFileDataFormat.java`/`ZipIterator.java`).
  **Still the top-priority human follow-up item.**
- **CAMEL-24299** (`XmlStreamDetector` DTD hardening, `core/camel-xml-io-util`, `NEEDS REVIEW`
  since 2026-08-04): still awaiting a human call on whether `XmlStreamDetector` is reachable on
  attacker-controlled XML in our deployment (vs. only via `camel-jbang`, unused). No change since
  yesterday.
- **CVE-2026-42527** (`camel-jms`, `NEEDS REVIEW` since 2026-07-22) and **CAMEL-23522**
  (`NEEDS REVIEW` since 2026-07-15): still awaiting human review; no new information surfaced
  today.

## Source health

- **Apache Camel security page** (`camel.apache.org/security/`): **FAILED** — `curl`: `CONNECT
  tunnel failed, response 403`; `WebFetch`: HTTP 403. Independently confirmed today via both
  tools, not carried over from prior reports.
- **NVD** (`services.nvd.nist.gov`): **FAILED** — same, confirmed today via both `curl` and
  `WebFetch`.
- **Apache Jira** (`issues.apache.org`): **FAILED** — same, confirmed today via both `curl` and
  `WebFetch`.
- **GitHub REST/Search API** (`api.github.com`, GHSA-by-ecosystem listing / releases / PR-commit
  search scoped to `apache/camel`): **FAILED** — `403` via both `curl` and `WebFetch`.
- **`apache/camel` git-protocol transport**: **OK** — anonymous clone/fetch of the public repo
  worked without issue (the session's git proxy serves this even though `apache/camel` is not an
  attached repo); used for the `main` sweep, `camel-4.8.x` check, and tag discovery.
- **`github/advisory-database` git-protocol clone**: **OK** — sparse checkout of the full 2026
  `github-reviewed` + `unreviewed` trees completed without truncation this run; full parity
  confirmed (0 new Camel-related advisories).
- **`raw.githubusercontent.com`**: **OK** (spot-checked).
- **Delivery webhook (Power Automate)**: see **Delivery** below.

## Delivery

See below — filled in after the delivery attempt.

## Recurring issue — needs owner attention

This is now well over **two weeks** of this environment being unable to reach
`camel.apache.org`, NVD, Jira, `api.github.com` (for the `apache/camel` scope), or the delivery
webhook host directly. The git-protocol and `raw.githubusercontent.com` substitutes have so far
kept coverage intact (confirmed again today via a full, untruncated GHSA-mirror parity check),
but this is not a substitute for the real thing indefinitely — Jira's own security-labeled issue
search still has no substitute, and the delivery webhook has now failed to reach the on-call for
what may be the third week running.

1. Review and widen this environment's outbound egress allowlist to include at minimum:
   `camel.apache.org`, `services.nvd.nist.gov`, `issues.apache.org`, `api.github.com`, and the
   Power Automate delivery webhook host.
2. **Confirm someone is actually seeing these reports.** If delivery has genuinely been failing
   since 2026-07-19 as these reports state, no email or Teams message has gone out for this watch
   in over two weeks — please check the `origin/main` git history / this repo directly rather than
   waiting on the webhook.
3. Have a human confirm and action the `camel-4.8.x` backport for **CAMEL-24293** (Zip/Tar Slip in
   `camel-zipfile`) — the upstream fix has been available for six days and is a small, mechanical,
   well-understood change (see "Outstanding items" above).
