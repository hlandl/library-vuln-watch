# Vulnerability Watch — 2026-07-26

**Eighth consecutive day** (2026-07-19 → 2026-07-26) that this run's execution environment
blocks outbound access to `camel.apache.org`, NVD, Apache Jira, the GitHub REST/Search API
(scoped to `apache/camel`), and the delivery webhook host — see **Source health**, unchanged
in kind from the prior seven reports. As on 2026-07-22 → 2026-07-25, the git-protocol/mirror-repo
substitute channel remains reachable and was used again.

Like the last two days, **today's full re-sweep found zero new items**: the
`github/advisory-database` mirror's entire 2026 tree (`github-reviewed` + `unreviewed`,
January–July) contains 16 genuine `org.apache.camel` Maven advisories (verified by parsing
each file's `affected[].package` field), and every one resolves — via its `aliases` CVE id —
to a fingerprint already recorded in `state/seen.json`. No new `apache/camel` git tag or
`camel-4.8.x` maintenance-branch commit since the 2026-07-25 check either.

## How today's data was collected

Direct HTTPS to `camel.apache.org`, `services.nvd.nist.gov`, `issues.apache.org`, and the
delivery webhook host all fail identically to the last seven days (`CONNECT` tunnel `403` via
`curl`; the proxy status endpoint (`$HTTPS_PROXY/__agentproxy/status`) shows no relay-failure
entries and a plain allow-list that does not include any of these hosts). `api.github.com/rate_limit`
(no repo scope) succeeds; `api.github.com/repos/apache/camel` and `api.github.com/advisories?ecosystem=maven`
both fail `403` — the session repo-scope restriction described in prior reports is unchanged
(this session is scoped only to `hlandl/library-vuln-watch`).

Workarounds used, same as the last four days:

1. **`git ls-remote`** against `apache/camel` (plain git transport, not repo-scope-blocked):
   newest `camel-4.8.x` tag is still **`camel-4.8.9`**; the `camel-4.8.x` maintenance branch
   head is still `5a31b932322777b8eb5f091cf9c6f016a62fe896` — both unchanged from 2026-07-23
   through 2026-07-25.
2. **Sparse, blob-filtered clone of `github/advisory-database`** (`--filter=blob:none`,
   sparse-checkout of `advisories/{github-reviewed,unreviewed}/2026`) — full 2026 tree checked
   out; 53 files mention "camel" (case-insensitive) anywhere in the record, same count as
   2026-07-25. Of those, 16 are genuine `Maven` / `org.apache.camel:*` advisories (verified by
   parsing `affected[].package`, not just the keyword hit — 3 more than the 13 counted in
   reports before 2026-07-25, which undercounted by matching on GHSA id alone instead of also
   resolving CVE aliases; recounted this run to be sure nothing was missed). All 16 resolve —
   by GHSA id or by CVE alias — to a fingerprint already in `state/seen.json`:
   `GHSA-qvc3-6q9x-95pj`→`CVE-2026-53913`, `GHSA-hcm8-x79p-wx2w`→`CVE-2026-48205`,
   `GHSA-f7g3-2cg6-f5hj`→`CVE-2026-48204`, `GHSA-8364-hfqj-pwm6`→`CVE-2026-47323`,
   `GHSA-c3f3-cc42-xr9v`, `GHSA-429q-mrc4-38fr`, `GHSA-5rc6-9qfp-8vwg`, `GHSA-vpr3-2659-rw55`,
   `GHSA-v3vg-332r-mw99`, `GHSA-4xwx-hvv7-7prj`, `GHSA-xfxp-ppx7-cqrp`, `GHSA-695c-x5gc-94gj`,
   `GHSA-jg2m-9x48-3gvj`, `GHSA-27vm-5vpj-rp5g`, `GHSA-4jrw-64vr-7g8m`, and
   `GHSA-2vqf-x7g4-7c2g`→`CVE-2026-33454` (camel-mail — already recorded `AFFECTS_US`). The
   remaining 37 files are the same false positives already documented in prior reports (Apache
   Camel K — different project/ecosystem; unrelated npm/Linux-kernel hits). None are new.
3. **`raw.githubusercontent.com`** — used to re-confirm the pinned-version (4.8.8) file state
   for spot checks; no new backport work needed since no new AFFECTS US finding surfaced.

This still does not substitute for the Apache Jira sweep (no *SUSPECTED*, no-CVE-yet findings
could be surfaced) or the GitHub PR/commit keyword search for silent hardening fixes (Search
API is repo-scope-blocked). `last_run` in `state/seen.json` is **left unchanged** at
`2026-07-18T04:11:09Z`, for the same reason as the last five reports: it should reflect the
last run that actually covered Jira and the PR/commit search, so that whichever run regains
access re-sweeps the full outage window for those two sources rather than just from today.

## Summary

✅ No new findings — full parity between today's git-protocol/mirror sweep and the findings
already recorded in `state/seen.json`. (Two pre-existing items remain open from earlier runs
and are unchanged: `CVE-2026-42527` and `CAMEL-23522`, both `NEEDS_REVIEW`; see prior reports
for detail — this run did not re-investigate them, per the methodology's dedup rule.)

## Findings

None new today.

## Apache Camel release / silent-fix check (git protocol)

- `git ls-remote --tags` against `apache/camel`: newest `camel-4.8.x` tag is still
  **`camel-4.8.9`** — unchanged since 2026-07-22, still more than 180 days old.
- `camel-4.8.x` maintenance branch head: `5a31b932322777b8eb5f091cf9c6f016a62fe896` — identical
  to the hash recorded since 2026-07-23; no new commits merged.
- Could not perform the GitHub PR/commit keyword search (`is:merged CVE`/`security`/
  `vulnerability`/`CWE` against `apache/camel`) — Search API is repo-scope-blocked. This
  remains the main coverage gap versus a normal run, unchanged from the last seven days.

## Source health

- **Apache Camel security page** (`camel.apache.org/security/`): **FAILED** — egress-policy
  block (`CONNECT` `403`), 8th consecutive day.
- **NVD** (`services.nvd.nist.gov`): **FAILED** — same block, 8th consecutive day.
- **Apache Jira** (`issues.apache.org`): **FAILED** — same block, 8th consecutive day. No
  Jira-only *SUSPECTED* findings could be surfaced this run.
- **GitHub API** (`api.github.com` — GHSA-by-ecosystem listing, releases, PR/commit search):
  **FAILED** — session repo-scope restriction, unchanged from prior days; `apache/camel` is
  not an authorized repo for this session.
- **GitHub web UI** (`github.com/apache/camel/*`): **FAILED** — same restriction.
- **`raw.githubusercontent.com`**: **OK** for known paths.
- **`apache/camel` git-protocol transport**: **OK** — used for tag/branch discovery.
- **`github/advisory-database` git-protocol clone**: **OK** — full 2026 tree re-swept, clean
  parity result today (no new Camel-relevant advisories since 2026-07-25).
- **Delivery webhook**: see **Delivery** below.

## Delivery

**Delivery failed.** Both attempts (initial + retry after 30s) to POST this report to the
configured delivery webhook were blocked by the same blanket egress-policy `403` described
above (`curl` exit 56, "CONNECT tunnel failed, response 403") — the webhook host could not be
reached at the network level, not merely rejected by the endpoint itself. No email or Teams
notification could be sent for 2026-07-26 via the normal channel; the on-call is being
notified out-of-band via a push notification from this session instead, and this report plus
the push to `origin/main` are the only record of today's run until delivery is retried. This
is the eighth consecutive day of delivery failure.

## Recurring issue — needs owner attention

Unchanged in substance from the prior seven reports: this scheduled routine has now gone eight
consecutive days (2026-07-19 → 2026-07-26) unable to reach `camel.apache.org`, NVD, Jira, or
the delivery webhook directly. Today's clean parity result again shows the git-protocol/mirror
substitute channel is not itself the problem — but Jira and the GitHub PR/commit search remain
completely uncovered for the whole outage window, a real, accumulating coverage gap for
*SUSPECTED* (no-CVE-yet) findings regardless of how well the mirror happens to be caught up on
any given day. Recommended actions, unchanged:

1. Review and widen this environment's outbound egress allowlist to include at minimum:
   `camel.apache.org`, `services.nvd.nist.gov`, `issues.apache.org`, and the Power Automate
   delivery webhook host.
2. Grant this session's GitHub scope access to `apache/camel` (via `add_repo`, with explicit
   owner sign-off) so the GitHub API-based sources (GHSA-by-ecosystem listing, release list,
   PR/commit search) are usable directly instead of via the improvised git-protocol/mirror-repo
   substitute.
