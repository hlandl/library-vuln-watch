# Vulnerability Watch — 2026-09-04

## Summary

| Library | Finding | Severity | Verdict | Official fix |
|---|---|---|---|---|
| Apache Camel | 11 new tickets swept from `apache/camel` `main` (2026-09-01 → 2026-09-04): LangChain4j agent/guardrail bug fixes, a Cassandra `KeyValueRepository` feature addition, a `camel-main` diagnostic-logging improvement, TUI/docs-only changes — none security-relevant, none touch a `used_component` in a security-relevant way | n/a | **NOT RELEVANT** | see below |

**No new AFFECTS-US or NEEDS-REVIEW finding this run.** 11 new fingerprints processed (all Jira tickets, 0 CVE/GHSA), all resolving to **NOT RELEVANT**. `findings` count for delivery = **0**.

## Findings

### 11 new tickets — swept, all NOT-SECURITY / NOT-USED

Window: `apache/camel` `main` commits 2026-09-01T04:08:56Z → 2026-09-04T04:03:44Z (48h overlap over `last_run` = `2026-09-03T04:08:56Z` per methodology step 1; 97 commits examined, keyword-grepped for `CVE|GHSA|security|vulnerab|CWE|exploit|traversal|injection|deserializ|XXE|SSRF|RCE|unauthoriz|authenticat|authoriz|sanitiz|escape|overflow|privilege|bypass|harden|contain|leak|credential|password|secret|expose|unsafe|certificate|TLS|SSL|crypto|symlink`, plus a full diff of every new Jira ticket id in the window against `used_components`). 30 of the 41 distinct ticket ids in this window were already recorded in `state/seen.json` from the prior two runs (including the `CAMEL-24548`/`CAMEL-24549` cloud-storage-containment hardening and `CAMEL-24578` Avro `ClassSecurityValidator` items, both unchanged since 2026-09-02/03); the following 11 are new:

- **CAMEL-23953** — Add batch operations for `langchain4j` embeddings. New feature, `camel-langchain4j-embeddings` is not a `used_component`. `NOT-SECURITY`.
- **CAMEL-24558** — `camel-main`: warn on unconsumed `camel.aiObservability.*` properties (e.g. the typo `enable` vs `enabled`) instead of silently ignoring them, matching the existing pattern for `camel.health`/`camel.errorRegistry`. Diff reviewed directly: purely a diagnostic-logging addition in `BaseMainSupport`, no change to trust/validation logic. `camel-main` **is** a `used_component`, but this is `NOT-SECURITY`.
- **CAMEL-24595** — `CassandraKeyValueRepository`: implement atomic `replace()`/`delete(key, expected)` using lightweight transactions. Part of the ongoing `CAMEL-24463` `KeyValueRepository` feature family; `camel-cassandra` is not a `used_component`. `NOT-SECURITY`.
- **CAMEL-24606** — `camel-google-firestore`: document the realtime change-type header. Docs-only, not a `used_component`. `NOT-SECURITY`.
- **CAMEL-24607** — `camel-langchain4j-agent-api`: `CodeInjectionGuardrail` was under-counting distinct injection-pattern types in non-strict mode (two patterns sharing one type, e.g. two `TEMPLATE_INJECTION` variants, were miscounted as "multiple types"), causing legitimate templating text to be falsely blocked. Diff reviewed directly: this **relaxes** an over-eager false-positive block to match the guardrail's documented intent — it does not weaken any check that gates real injection attempts, and is a bug fix rather than a new vulnerability. `camel-langchain4j-agent-api` is not a `used_component` regardless. `NOT-SECURITY` / `NOT-USED`.
- **CAMEL-24608** — `camel-langchain4j-agent-api`: enforce `LanguageGuardrail` `minLanguageRatio`. Guardrail feature refinement, not a `used_component`. `NOT-SECURITY`.
- **CAMEL-24610** — `camel-langchain4j-web-search`: guard against a null `maxResults` when a custom `WebSearchRequest` omits it (NPE fix). Not a `used_component`. `NOT-SECURITY`.
- **CAMEL-24611** — `camel-chatscript`: fix `resetChat`, `chatUserName`, socket timeout, and non-`String` body handling. Functional bug fixes, not a `used_component`. `NOT-SECURITY`.
- **CAMEL-24612** — `camel-langchain4j-agent`: fix content-converter charset, content-type normalization, and stream close. Functional bug fixes, not a `used_component`. `NOT-SECURITY`.
- **CAMEL-24613** — `camel-langchain4j-agent-api`: `LanguageGuardrail` was rejecting plain English when `allowMixed=false`; false-positive fix, not a `used_component`. `NOT-SECURITY`.
- **CAMEL-24617** — TUI: add a YAML DSL refactoring menu (Ctrl+R) to the Source tab. Dev-tool feature, not a `used_component`. `NOT-SECURITY`.

No un-ticketed commit in the window matched the security-keyword grep.

### WebSearch general advisory sweep

Ran the same broader WebSearch queries used since 2026-09-02 as a substitute for the still-blocked GHSA/NVD/Jira/security-page APIs (queries: recent Apache Camel CVEs, `camel.apache.org/security/` advisory pages, "new CVE this week"). Candidate CVEs surfaced: `CVE-2026-33454`, `CVE-2026-33453`, `CVE-2026-46456`, `CVE-2026-49042`, `CVE-2026-43865`, `CVE-2026-40453`, `CVE-2026-49098`, `CVE-2026-53913`, `CVE-2026-40473`, `CVE-2026-40858`, `CVE-2026-40048`, `CVE-2026-40860`, `CVE-2026-46457`, `CVE-2026-46726` — **all already present in `state/seen.json`** (recorded on runs between 2026-07-15 and 2026-08-06, verdicts unchanged). No new CVE/GHSA fingerprint surfaced this run.

## Carry-over check

Re-checked the four open `NEEDS_REVIEW` carry-overs (`CAMEL-24296`, `CAMEL-24298`, `CAMEL-24299`, `CAMEL-23522`) via WebSearch for a newly-assigned CVE/GHSA: none confirmed. One search hit returned a `CVE-2026-24296` CVE-record page (numerically similar to `CAMEL-24296`), but no source ties it to Apache Camel or to that specific Jira ticket — treated as an unrelated coincidence of numbering, not a match. No fingerprint or verdict change; all four remain `NEEDS_REVIEW`.

## Method notes

- **Lookback window:** incremental mode, 48h overlap over `last_run` = `2026-09-03T04:08:56Z` → swept since `2026-09-01T04:08:56Z`, per methodology step 1.
- **`apache/camel` commit sweep:** same substitute source as every run since ~2026-07-19 (GHSA/NVD/Jira/security-page direct HTTP access remains blocked — see Source health). Anonymous shallow clone of `main` (blobless, then `--depth=1000` fetch to cover the window with margin), 97 commits in range.
- **`camel-4.8.x` maintenance branch:** head unchanged at `5a31b932322777b8eb5f091cf9c6f016a62fe896` (same as every run since 2026-07-23, confirmed via `git ls-remote`) — no backports landed there.
- Repository access: `apache/camel` was attached read-only for this session (anonymous git clone/fetch); the GitHub REST/GraphQL API and MCP GitHub tools remain unavailable for it (session's API gateway only serves the explicitly-attached `hlandl/library-vuln-watch`), so `api.github.com` (advisories, releases, PR/issue search) is not usable — same limitation as prior runs, worked around the same way (git log substitute + WebSearch).

## Source health

- **camel.apache.org, issues.apache.org, services.nvd.nist.gov, seclists.org**: **FAILED** — egress proxy rejects the CONNECT (`403`, policy denial), reconfirmed this run with direct `curl`. Same failure mode reported in every run since ~2026-07-19 (now 7+ weeks).
- **api.github.com** (GHSA REST endpoint, and `apache/camel` releases/PR/issue search): **FAILED** — `403` via direct `curl`; not usable via the session's repository-scoped API gateway either (`apache/camel` is not an attached-with-push repository).
- **github.com git protocol (`apache/camel` clone/fetch)**: OK.
- **WebSearch**: OK — used for the routine commit-sweep cross-check, the general advisory-corpus sweep, and the carry-over check.
